# Buildr Service

## 职责

Buildr Service 是 Product Project 的可执行应用实现，负责 Bootstrap composition与公共CLI Host、System Installation、Workspace/Project/Service/Task Record/Task Environment/Task Review/Task Retrospective/Task Verification/Task Development/Parent Coordination/Task Finish/项目每日演进 domain、Skill-only Git Operations、Project Testing 指导、Buildr Web Runtime 与 session 同源托管、runtime adapters、受管资产与 Component 生命周期、capability graph、验证执行、package 和发布。Buildr Web React/Vite 权威前端源码与构建属于 sibling Buildr Web Frontend Service `buildr-web`；本 Service 在构建/打包时消费其产物到顶层 `web-dist/`，运行时不依赖 `buildr-web` 源码或 Vite。

## 接口与入口

- 发布身份链固定为 `dev baseline → ordered selection chain → release HEAD/tree → main reconciliation provenance（可选）→ Product Candidate generation → frozen tarball manifest/integrity → generation carrier → main merge commit/tree → post-publication dev provenance reconciliation → closeout → transaction evidence`。current Product已经实现selection、Candidate/artifact、Task correlation、shared readiness/protected transaction、Git reconciliation与closeout owner；任一owner无法回读current fact时仍返回`release-model-implementation-incomplete`。已成立的Publication与后续`published-but-dev-reconciliation-blocked`维护失败保持正交。
- release lifecycle projector从current owner facts派生version-scoped阶段与稳定recovery identity，不写Task Record新状态或旁路workflow store。唯一`release-<version>`协调Task从selection到Publication、dev provenance reconciliation和必需closeout全部成立前保持active；support Task delivery、Candidate或readiness通过都不使其completed。
- `release-orchestration-runner.mjs`提供`prepare-dispatch`、`dispatch`与`closeout`三个前向恢复动作。前两者复用transaction readiness/protected dispatch owner并保留维护者对current context digest的显式publication授权；closeout按hosted evidence、dev provenance reconciliation、Git closeout、lifecycle closed、retained Task no-change completion、retained Environment cleanup与retained Doctor推进。每步继续由原owner持有成功事实与授权，编排器不建立持久化状态或跨owner原子事务；Task已terminal时只恢复cleanup/Doctor。
- `release-phase-timeline.mjs`从current owner facts生成portable `buildr.release-phase-timeline/v1`与稳定identity，并直接把failed-shard retry Result的`runId`、`runAttempt`、`failedShards`及aggregate `evidenceAttempts`投影为多attempt事实。Timeline按可证明边界区分`machine-execution`、`platform-queue`、`environment-approval`和`human-decision`，缺少任一边界时不估算duration；Candidate以`runId + runAttempt`表达实际rerun scope、每个evidence的原attempt与最终aggregate identity。compact只保留关键阶段、identity与inspect pointer，full才展开owner facts。
- release task evidence correlation只读取Task、Environment、Development、Finish与matching self-bootstrap的current read model，生成portable carrier/context identity；不复制专业Result、不扩展Task Record/SQLite writer。

- Release transaction runner从selection、Candidate aggregate/唯一artifact、Task correlation、Task Environment/exact Node与Git/GitHub readback构造`buildr.release-context/v1`。`release-transaction-evidence.mjs inspect-run`按publish run校验artifact identity与GitHub source/attempt，返回portable关联，不扩张Task Record或SQLite schema。
- 同一source SHA的Candidate暂态失败由`candidate-failed-shard-retry.mjs`唯一入口先核验matching workflow run与terminal失败shard，经维护者显式确认后只派发GitHub failed-jobs rerun。新attempt的aggregate可以复用同一run内已通过attempt的逻辑artifact，但必须回读run与aggregate attempt identity；新SHA不得复用旧evidence。
- 长流程CLI统一以closed`buildr.long-running-operation-summary/v1`作为缺省stdout投影；各专业owner由自身Result或hosted/output evidence持有完整事实。compact不是新authority，stdout丢失、超时或展示截断只能触发同一owner inspect。
- `release-git-convergence.mjs`分别拥有release→main、Publication后只读dev provenance reconciliation、必需closeout与可选正式ref删除的窄动作。release→main为current generation使用确定性`codex/release-main-<version>-g<generation>` carrier并确保唯一受保护PR；`reconcile-dev`核验passed Publication、current frozen selection、正式remote release/main refs，以及baseline和全部`sourceDevCommit`仍由current remote dev包含，返回空effects且不读取merge policy、不创建worktree/commit、不push dev。来源或identity漂移返回`published-but-dev-reconciliation-blocked`和稳定recovery identity且不撤销Publication；旧`converge-dev`仅是同一只读实现的兼容入口。closeout保留并核验正式远端`release-<version>`，只删除identity匹配的carrier、本地selection branch/refs与owned worktree；未知ownership fail closed。正式ref删除仍是独立可选授权，不阻止lifecycle closed。
- CLI：`projects/product/buildr`是Buildr checkout唯一开发入口；PATH默认`buildr`只代表npm installation。两者身份和更新责任隔离。`bin/buildr.mjs`只委托`src/bootstrap/cli/main.mjs`；Bootstrap创建同一Runtime/module set并合并唯一command catalog。Task Record/Task Review commands来自Task module contributions；`project daily-progress record|inspect|list` 来自Workspace module contribution；`update`、`installation status`与`web launcher install|status|repair|uninstall`只来自System Installation module contribution，其余命令保持在公共Host与未迁移Adapter边界。每日演进 `record` 校验 closed v2 payload 后原子覆盖 `.buildr/daily-progress/<project-code>/<YYYY-MM-DD>.yml`；Task 关联可选，他人提交禁止挂 Task，存在的 Task ID 仍须本机已有。`inspect`/`list` 只读。JSON 使用 `buildr.project-daily-progress-*-result/v1`。Application 不扫描 Git、不写 Task SQLite、不提供 cron；收集 Git 由产品 Skill 在写入前完成。
- Release Awareness Application一次读取npm的`latest`与`next`，分别投影GA正式版和RC候选版。`buildr update check [--json]`同时展示两个版本；`buildr update --track stable|candidate`只在用户显式选择后更新精确版本，不自动切轨或降级。
- Buildr Web：loopback HTTP 与浏览器界面。Task 列表默认 `open` (todo + active)，可分别过滤 todo、active 和终态；Task 详情展示复盘来源，复盘 Tab 展示后续 Task 与当前状态。正式 Task 只由 Agent/Task Manager 创建或激活，Buildr Web 只编辑/结束已有 Task 并维护复盘处置。六个 Task 一级视图包含独立“原型”Tab；Change Application 只在 Task 已有关联 Change 内有界发现全部带 `buildr:ui-prototype` 标记的自包含 HTML，`/ui-prototypes` 列表 JSON 只返回 portable metadata，具体内容只由 Task 与不透明页面 ID 的专用只读响应返回并强制 opaque-origin CSP sandbox。变更前的标记与接口不兼容。Runtime 不接受文件路径、不提供任意 HTML route、不扫描无关联 Workspace 路径，也不取得 UI Prototype writer；Task-scoped Change route、其他专业只读边界、复盘处置 PATCH 与文章入口语义保持不变。
- Project Document Application 为 Buildr Web 提供 Project-root 内受限 `.md` 读取。Task Intent 中的 Workspace 相对文档链接由前端先根据 Task scope 和 Project registry `source.path` 定位 Project，再复用 `GET /api/v1/projects/:project/documents/<path>` 只读展示；不扩展 Task Record schema，不引入附件表或第二文档 writer。
- Daily Progress 是 Workspace module 中的 Project-scoped 纵向切片。`src/workspace/module.mjs` 私有组装唯一 YAML Repository/Application 并贡献 CLI/HTTP Adapter；公共 Bootstrap/HTTP Host 不直接实现其业务路由。Application 拥有本机每日演进 YAML 的唯一 writer/reader。HTTP 只开放 `GET /api/v1/workspaces/:workspaceId/projects/:project/daily-progress[/:date]` 与 `GET /api/v1/workspaces/:workspaceId/tasks/:taskId/daily-progress`；不接受 `target`/`path`/`root`，不提供 POST 写入，也不在 GET 时执行 `git log`。读取时解析本机 Task 标题与状态，消失的 Task 标 unresolved 且不改文件；v1 文件返回 incompatible。
- Resources：`services/buildr/resources/manifest.yml` 定义发布边界、可安装产品内容、builtins、contracts、bindings 和 Components；`resources/workspace/` 是受管 Workspace 内容的 source authority，不携带 `.buildr/workspace.yml`、Workspace 根 Registry、Project capabilities/commands 或 Service Registry 等用户持久化配置源。`init`、Project create 与 `sync` 使用 canonical Domain renderer/writer 生成缺失 Registry并保留已有用户内容，再从资源声明收敛 Builtins/Components。Component definition 同时拥有 Skill fragments 与其引入的结构化 capability dependencies，builtin descriptor 不重复维护 Component-owned `requires`。
- Workspace module 的 source-root Domain 统一解析 Project/Service Managed Root 与 Attached Root。`project add --attach`、`service add --attach` 只登记已存在的独立 Git top-level；Application 在写 registry 前核对绝对 realpath、remote、branch、Workspace 包含关系和重复来源，且不修改外部 repository 的内容、HEAD 或配置。Project/Service detail 暴露 root kind、实际位置、ownership 与 Git identity；Change/OpenSpec、Publication、Verification、Task Verification 和 Doctor scope 通过同一 resolver 消费实际来源。Service registry 始终属于实际 Project root；需要 Workspace ownership 的 action 不因 attach 自动取得外部 writer authority。
- Runtime Skill renderer统一组合package产品入口与Workspace Skills。产品入口不接收完整capability routing dump；每个consumer只获得自身`requires`的紧凑binding block。Doctor full输出全局capability graph，destination-aware `.buildr/agent-runtime/` receipt保存受管文件identity和consumer-local contract digest/provenance快照，runtime目录不承担机器证据authority。
- Agent Assets 平台实现已迁入`src/agent-assets/`：Rule、Skill、Command、Component、Builtin/package maintenance与runtime projection位于`application/`，Agent adapter、Capability Binding、render、projection和receipt位于`infrastructure/runtime/`，并只由`agent-assets/module.mjs`向Bootstrap注册。旧`application/domains`资产入口、`application/package-maintenance*`、`application/runtime.mjs`和全局`infrastructure/runtime/`已退出。Workspace manifests与内容仍是源资产，Agent原生文件和`.buildr/agent-runtime/` receipt仍是可重建投射；产品入口Buildr Skill、Workspace Builtin与package runtime source的长期合并关系继续待后续产品决策。

## 应用负载、Launcher 与发布运行时

- `tools/release/application-payload.mjs`把同一runtime CJS bundle、read Worker bundle、Web dist、migrations、resources、deferred package runtime、生产依赖、图标与许可证冻结为`buildr.application-payload/v1`；npm staging消费同一`applicationPayloadDigest`。resource resolver只从摘要验证payload或canonical development root解析资源，正式runtime不依赖cwd、PATH、`buildr-web`源码或Vite。`tools/`与`test/`不进入npm tarball或Application Payload，产品runtime也不得导入它们。
- Candidate packaging 在唯一 `release-artifact.json` 中额外绑定 source commit、source tree 与 Candidate generation；`candidate-ci` 的每个 shard、Host Node tuple、Windows/Launcher consumer 和 publish smoke 都只接受 matching identity，失配时在任何公共 mutation 前 fail closed，不重新 `npm pack`。
- npm installation identity、origin/registry、CLI update、installation status、npm lifecycle enrollment、Launcher binding 与正式 npm Launcher install/status/repair/uninstall 由 `src/system/installation/{domain,application,infrastructure,interfaces}` 及唯一 `module.mjs` 拥有；`domain/release-version.mjs` 是 Release Awareness 与 release tools 共用的 SemVer/track authority。Bootstrap、Application Payload、Web 与 Doctor 通过公开模块入口或注入的 identity/binding/Application/Diagnostic ports 消费同一实现；旧 application、product-identity、product-launcher 与 CLI Launcher 入口已删除。通用 Product Data Root、filesystem、process 与 product-resource 机制仍归全局 Infrastructure。

- Buildr Web Runtime Host 已收敛到 `src/web/`：实例生命周期位于 `application/`，PID、锁、端口、进程和目录选择适配位于 `infrastructure/`，CLI 位于 `interfaces/cli/`。`http/server.mjs` 只拥有 loopback/listen/close 与资源生命周期，`router.mjs`、`session.mjs`、`static-files.mjs`、`responses.mjs` 分别拥有路由、安全请求、静态托管和响应映射，bounded read executor 保持独立。公共 Host 只分发各模块提供的 HTTP contribution，不实现 Workspace、Task、Change、Publication 或 Installation 业务路由。System Doctor 的 CLI、Application、结果模型和诊断位于 `src/system/doctor/`，并在 Bootstrap 末端聚合各模块 Diagnostic/Read Model contribution；它保持只读且不取得业务 writer authority。公开 CLI、HTTP、JSON、SQLite schema、migration、Session/Origin/Secret、实例复用与 Preview 行为不变。
- Public JSON identity/envelope 与 Declaration Intake next-action contract 由 `src/infrastructure/contracts/` 拥有；同目录的 `json-schema-validator.mjs` 只提供通用 strict Draft 2020-12 Ajv 编译/复用机制，不拥有业务字段语义。Task list/detail/update/complete/abandon 已作为首个 HTTP 契约参考切片：Schema 与 operation catalog 位于 Task HTTP Interfaces，校验后的 DTO 显式映射到既有 Application，构建工具从同一 authority 生成两端 tracked TypeScript DTO并检查 drift，真实 HTTP Contract Test校验成功/错误响应。其余 route 仍按模块逐步迁移，未覆盖只形成非阻塞诊断。Task internal workflow catalog/router 位于 `src/task/contracts/` 与 `src/task/interfaces/internal/`，由 `task/module.mjs` 注入 runner 后供 Bootstrap 调用。Git Worktree CLI Adapter 同样归入 `src/task/interfaces/cli/`。顶层 `src/application`、`src/domain`、`src/interfaces` 已无生产文件。
- Task professional HTTP覆盖Task Overview、Environment、Development、Review、Verification、Parent Coordination与Retrospective的实际读写操作，不公开Task Entry Snapshot、Task Execution Record或Review/Verification后端prompt route。Review、Development和Verification GET分别调用自身Application；Agent指令由前端和Skill基于当前Task形成，Terminal Delivery只读取Task Record与Finish history。
- Runtime/System HTTP Schema authority 分属 `src/web/http`、`src/system/installation/interfaces/http` 与 `src/system/publication/interfaces/http`：health、两种 quit、release-awareness、Publication JSON 与 binary asset 均有稳定 operation metadata、strict validator、真实 HTTP Contract Test 和统一错误契约。构建期从同一 Schema 生成 Buildr/Buildr Web tracked DTO并检查 drift；全局 coverage 保存 migrated JSON、binary、deferred 与 non-HTTP disposition，离线暴露未知/重复 operation但不参与Runtime启动。Publication binary bytes/MIME/path安全、session/Origin/instance secret、shutdown顺序以及 Doctor/Launcher/release writer ownership保持不变。
- npm package是唯一正式产品安装。`buildr web launcher install`只在显式请求后生成本机macOS `.app`或Windows Start Menu shortcut，v2 closed binding精确记录formal npm origin、Host Node、package entry、prefix、payload/protocol、target与`webPort`策略；默认首选`4457`，install/repair可指定`0..65535`，`0`直接随机。macOS wrapper同步校验后向用户launchd提交一次性runner并退出，bundle identifier由Launcher ownership派生；launchd后代从binding的绝对Host Node重建PATH首项，并把实际executable、version与PATH head写入Launcher log。默认release smoke使用隔离Root直接执行无界面入口，强制no-open/no-notify并清理owned进程；真实平台启动入口集成仅由显式专项运行，不进入affected/full/Candidate。每次Launcher startup使用15秒独立readiness budget；失败时先保留脱敏诊断，再清理owned进程与临时安装根。真实用户未设置no-notify时仍保留可见失败诊断。wrapper与runner不复制Node、Buildr package或payload，也不形成独立更新渠道。
- Development Launcher继续由当前checkout的`package/launchers`内部manager构建为thin入口，只包含图标、identity与启动脚本，绑定Service source root、精确development Node与固定端口`4458`，不复制payload或npm安装。`npm run install:development`和self-bootstrap activation都直接调用manager，不经过npm-owned公开`buildr web launcher`命令。同profile健康实例按单实例语义复用；foreign进程占用`4458`时明确失败，不随机回退、不停止占用者。Development Launcher与formal npm Launcher在名称、target、identity和lifecycle上隔离。
- 普通 Web Runtime 由 installation channel 与 runtime role 唯一解析 released/development Web profile。两者分别拥有默认 Web Data Root、Workspace registry、instance/start lock和日志，可并行运行且不跨 profile 复用或停止；Development Launcher固定使用`4458`，直接CLI与Task Preview仍可使用显式或随机loopback端口。released由正式Launcher启动时读取binding策略，非零首选端口因`EADDRINUSE`失败只在同一start lock内随机回退一次。其他单实例、ownership、profile隔离、receipt清理、Product Data Root与Frontend标题语义保持不变；旧 released Root 中的 development instance 仍以 profile conflict 报告并保留。
- Workspace 注册、`web --target`、Launcher 与 central Structured Store open 共用 Workspace management fence：先核对 real root、Workspace UUID、对侧 registry 和 `.buildr/local/web-management.json`，再允许任何 SQLite 创建、migration 或写入。错误 channel 条目只能从当前 registry 精确移除；该动作不打开/删除 SQLite，且无 force/adopt。Preview 保持按实例名隔离的 Root，不取得普通 management claim，并把子进程输出写入自己的启动日志。
- `installation status --json`、Doctor和Web health/instance分别展示receipt证明的npm、development、Launcher与current instance identity；`update`按同一receipt路由npm package或development checkout，并只原子刷新同slot已有Launcher，不扫描PATH、文件名或目录形状。
- Doctor通过独立`releaseAwareness`/`notices`投影版本提示，查询失败不进入Workspace findings、repair plan或readiness；Buildr Web Runtime只开放全局只读`GET /api/v1/release-awareness`，不提供网页npm更新mutation。每轨道的seen/notified/check时间只保存在用户级Buildr Data Root，不写Workspace。
- Doctor finding 以 `domain`、`scope`、`affectedActions`、`ownershipUnit` 表达局部影响，JSON 额外输出 `domainHealth`；兼容 `health` 明确以 `generalWorkPermitted: null` 拒绝成为通用许可。sync source plan 只把 required Component conflict 作为当前受管 mutation 的 blocker，optional/foreign-owner unit 保留 warning 并跳过其独立写入；Capability route 与其他 consumer 继续按各自 required/optional contract 判断，不从聚合 Doctor readiness 扩张授权。

## 数据与依赖

- Workspace/Project/Service、Rules、Skills、Commands 和 Components 使用 YAML manifests/registries。
- Workspace Structured Store使用每个Workspace root自身的`.buildr/local/workspace.sqlite`，由随npm package交付的连续SQL migrations、checksum ledger和Doctor管理。它是Buildr Local单机数据，不进入Git或同步；未来Server/Cloud不复用数据库文件作为共享协议。Buildr Web registry已解析root后的read-only Application path只读取该root，不执行Git/worktree provenance观察或`git rev-parse`；writable、migration和mutation path仍执行writer guard。自举 candidate runtime 在与 retained Workspace 共享 Git common-dir 时，只能写入自己的 linked Task Validation Workspace store；写 retained canonical store 或 peer checkout 会在创建目录、SQLite、WAL/SHM、ledger 或业务 row 前被 provenance guard 拒绝。验证 store 从完整 migration chain 建立，数据不回灌主库；最终集成后的 retained runtime 才能升级主库。
- Task Record 使用 closed `buildr.task-record/v2`，由`src/task/module.mjs`显式接收Structured Workspace Store、Project/Service Reader、Change Resolver、operation memoizer和Parent Coordination Reader，在私有组装对象中创建唯一Repository/Application，并贡献自身CLI/HTTP Adapter。Bootstrap兼容Facade只让既有消费者继续访问同一Application与窄Persistence读取面，不复制数据库或writer；其owner、冻结调用者和`legacy-exit-and-conformance`退出条件由架构验证约束。状态、todo规则、复盘信源、Parent/Child、scope/Change、单事务writer、linked-worktree guard和旧`task.yml` inert边界保持不变。
- Task Review 使用 Workspace SQLite 中的`(task_id, planning|completion)`两个可选current slots与closed `buildr.task-review-result/v2` schema。Result只保存subject identity、真实method、reviewed/uncovered、findings、局部结论和完成时间；`inspect`只返回保存事实，不推导current/stale。`record`要求`absent|resultDigest`并在同一SQLite写事务中比较后整值替换，冲突零写入。Application不生成prompt、不依赖Development/Candidate，也不成为Development、Verification、Finish或Parent门禁。v1 current rows一次迁入v2，不双读、不建历史表。
- 任务研发保留独立研发事实和历史交接；父计划、贡献绑定、贡献登记及父验收写动作已退役，不作为新协调的前置。
- Task Retrospective仍以单一SQLite current row保存原始Markdown与`pending|handled|no-action`。Provider从Task时点、专业current摘要、Finish timing、工具结果与已有current报告建立有界执行事实图。
- Task Verification v4把Project`verification.yml`作为测试地图。开发中的验证由Agent直接调用项目工具且不写Task报告；开发完成后Task Verification Application只提供`record|inspect`，保存实际checks、gaps和结论。
- Task Development使用Workspace SQLite中按Task ID唯一的closed`buildr.task-development-receipt/v3` current row。Planning snapshot与Content Target current后即可freeze Candidate；`knowledge` action保存current tree与`aligned|not-applicable|attention|blocked`最小disposition，只有blocked阻止proceed/handoff。Task Development不依赖Task Review或Task Verification，不读取两者Result，不派生跨专业准入，也不保存新的planning/completion/verification gate；旧Receipt/Handoff gate只作历史decode，新写入清空。
- 多Project Current Knowledge必须逐Project形成最小disposition，并在`knowledge` action一次提交精确Project集合；Application确定性派生顶层status、source identities与unresolved items。单Project和仅工作区旧聚合输入继续兼容，多Project缺项不能满足proceed/handoff。
- 父子管理从任务记录直接读取目标、关系及子任务结果，旧专用计划只读。父任务完成要求当前观察、总体验收、逐项处置与明确用户授权；界面和命令共享同一写入保护。见[父子管理](../flows/parent-child-management.md)。
- Task Execution Record已整体退役。升级迁移删除表和明确owned的本机正文目录；CLI、HTTP、Web、恢复、配额、retention和GC均不存在。以后出现真实长流程恢复需求时，由具体owner按实际需求设计。
- Buildr Product的registry/planner是Project自有测试架构，不属于通用Task Verification Application，也不产生Task Execution Record。

Task Record与各专业模块共用`.buildr/local/workspace.sqlite`，但不合并Domain、Application、writer或状态机。已退役的`task_lifecycle_current`不再复制跨专业事实；Task Overview只并列各owner最小查询事实，Parent Coordination只读Task及Task-owned旧Parent Plan历史，Terminal Delivery只读Task Record和Finish history。它们不新增store、cache、view、progress/lifecycle/event/history表或writer。项目每日演进不进入该 SQLite，只使用 ignored YAML。Task current records不发布、不进入Git、不进行本地多机同步；Task Metadata Publication、静态Task Board与旧Task Asset Review生成链均已清退。既有静态页面与`.buildr/asset-review/`只作为历史数据保留，不由Service读取或维护。Environment与Finish继续拥有各自本机运行/交付事实，Buildr Server/Cloud只作为未来团队协作方向。
- OpenSpec 依赖 `@fission-ai/openspec` 1.6.0；apply 前使用上游 strict validation、Buildr语义preflight与Task Planning Identity，Planning Review由Agent按目标独立选择而不是门禁；Buildr补充跨 Change conflict evidence、文件事实驱动的确定性收敛事务和 runtime contributions。`baseline create`、阶段型 `check`、`sync-plan`、`sync-apply` 公开 CLI 已删除，deterministic primitive 只在 `converge` 事务内部组合；历史 archive bytes 保持 inert，不作为当前授权。完整`MODIFIED`省略既有唯一Scenario identity时，planner继续整批零写入返回`semantic-resolution-required`，并以`scenario-identities-omitted`和确定排序的`omittedScenarioIdentities`给Agent最小可移植上下文；Buildr不复制正文、不自动补回或决定删除。
- Buildr Web Task API先把已登记`workspaceId`解析为canonical root。Overview、Development、Environment、Review、Verification、Coordination与Retrospective分别调用自身Application；没有Execution Record route或通用历史执行浏览器。
- Buildr Web的专业Task只读操作通过固定容量bounded read executor执行；Worker只接受overview、development、reviews、verification与coordination closed operation。
- Buildr Web 实例生命周期是独立后端模块：`src/web/application` 编排默认实例、Preview、Launcher handoff、maintenance和清理，`src/web/infrastructure` 管理Web专属receipt、启动锁、健康探测与认证退出，`src/web/interfaces/cli` 通过 `src/web/module.mjs` 向统一CLI Host贡献commands。模块内技术层默认扁平，不为默认实例、Preview或maintenance建立单文件能力目录。HTTP Router、Session、安全边界和`web-dist`托管暂留既有HTTP Host并通过server factory接入，不由本切片改写；React/Vite源码和正式前端构建继续归`buildr-web` Service。
- Project根可选`preparation.yml`使用closed`buildr.project-environment-preparation/v1`长期声明Project-wide或Service-scoped Recipe。Task Environment只约束实际消费Buildr-managed checkout、Preparation、runtime projection、Task-owned资源、正式环境证据或cleanup的动作，不把Formal Task或普通编辑/构建/有界测试变成通用许可。Agent选择受管路径时按Task scope提交Plan Request；`task environment prepare`必须带`--agent <adapter>`，省略失败且不默认为Codex。Application保存Plan v3与Receipt v6；required Recipe/Step缺失、漂移或失败只阻塞相应受管动作。
- 多Project Formal Verification的辅助Preparation不通过局部Plan Request依次累积。Task Verification从current完整Plan集合生成精确requirements闭包，Task Environment仍以一次closed request整值替换Plan、移除陈旧requirements并幂等复用current Steps。
- Git worktree provider 只在 Git common-dir 保存 `buildr.git-worktree-evidence/v1`，包含 repository plan、checkout/branch/HEAD/clean/registration 和 Git effects。`worktree create|inspect|cleanup` 不返回 Environment ready、Runtime/CLI/依赖、资源、恢复或总 cleanup 结论。
- Git Operations 只由 `git-operations` Skill 和 `buildr.git-operations/v1` contract 表达调用期行为，不新增 Application、CLI、Receipt 或 store。默认提交语言由随包 workspace `AGENTS.md` 提供，Project、Service、repository 更具体约定优先；Git Operations 与 Task Finish 只消费该规则，不复制 Core 语言契约。consumer 提供 repository/operation/ref/scope/授权；Result 只返回适用的前后 identity、完整 push range、变化维度和实际 effects。
- 普通 Rule/Skill 内容修改不要求新 session、reload、re-enter 或 activation evidence。候选 CLI/runtime 可以在自身 Task Validation Workspace 验证；只有集成到 retained source 后正式 runtime 才同步生效。bounded Agent capability 必须由 Agent 形成事实，文件投射或 Environment ready 本身不等于验证通过；Result 不复制 Environment Receipt。

Task Environment不再建立Workspace级Node probe或execution binding；`workspace-foundation`仅从当前执行环境解析Preparation Step明确命名的命令并记录executable identity，Project/Service executable继续按既有scope约束解析。Agent运行测试时直接使用当前项目与工具环境；Task Verification Application不解析或替换node/npm/npx，也不把Node事实写入任务验证报告。Task Finish不再冻结、核验或投影Workspace Node identity。

## 运行与验证

唯一verification registry对target至少15秒的日常Integration/System owner保存公共结果、确定性反例、保留真实边界与审计处置；planner派生27项primary evidence map并在执行前验证闭合。2026-08-24现场plan-only显示daily-full为52 steps、1,036秒目标工作量与259秒数学下限，Product Artifact Candidate为66 steps、1,398秒目标工作量、349.5秒下限与唯一tarball。Candidate CI的`core-*`仅为macOS平台shard命名，不是daily-full membership。

本Service的通用技术机制由`src/infrastructure/index.mjs`统一组装；`workspace-sqlite.mjs`是SQLite connection、operation scope、全局migration、锁与事务的唯一owner。Task Execution Record Persistence已删除。

Task Entry Snapshot与`task next`已经删除。智能体（Agent）读取当前Task、现场和对应Skill，自行选择Environment、Development、Review、Verification、Parent或Finish接口；Application不维护统一下一步或跨专业准入。

随包`task-manager`只维护Task Record及明确的Parent/Child关系，不调用Environment、Development或Review writer。父子管理直接读取Task目标、关系和真实子任务结果；旧Parent Plan由一次SQLite migration复制到`tasks.legacy_parent_plan_json`供只读历史展示，原Development payload逐字保留。旧`task parent record|reconcile|bind-child|refresh-planning|reconcile-child-delivery|accept`入口及Development current fallback已经删除。

Task Record以Domain/Application/SQLite repository构成独立writer；Task Environment、Task Review、Task Retrospective、Task Verification与Task Development继续使用各自Domain/Application/repository。它们只共享Task ID，Task Record不再以Task目录或文件存在作为authority。Task Development只通过bundled Skill和matching retained controller的`__internal task-development` route执行`begin|planning|observe|knowledge|freeze|decide|handoff|carrier` actions；同一runner另外提供无需Task/Workspace且不compose runtime的全局/action `--help`、action `--schema`与`--example`，其closed顶层输入字段来自同一action contract，运行态合法性仍由Application判断。`begin|planning`的required fields由Application从该contract执行写前校验，省略完整`planning`整值会在写入前失败，显式空snapshot仍合法；其他action保留既有Application/Domain诊断顺序。普通action可opt-in `--compact`，从同一次完整operation result投影current identities/applicability、effects、diagnostic与建议性next actions，不追加观察、不形成第二authority，也不根据效率指标自动推进。Task Retrospective通过同一retained invocation的`__internal task-retrospective` route执行`record|inspect|list|handle`；Task Planning Identity通过只读`__internal task-planning-identity` route解析语义target。统一route inventory由package static validation与Doctor检查runner、source wrapper和全部受管consumer闭环，npm installed-layout tests在无checkout source和空`NODE_PATH`下真实启动三个route。`list`复用Task Record current query与单份inspect语义，按处置状态或显式Task集合返回有上限的稳定摘要，只有显式请求才包含报告正文，且不建立缓存、分析聚合或mutation。Agent收到宽泛处理请求时只读inspect并展示拟disposition、理由和Task effects，只有用户直接指定完整mutation或明确接受未变化方案后才执行Task关系与handle写入；Buildr Web只新增同一Application所有的处置mutation。三者都不注册公共CLI。`git-operations`、`task-environment`、`task-review`、`task-retrospective`、`task-verification`、`task-development`与窄`task-worktree`分别提供对应独立capability。

Service source使用Node.js ESM并支持`>=24.15.0 <25`；Product checkout以`.node-version`锁定精确development Node `24.15.0`。后端TypeScript采用`strict`、`NodeNext`、`verbatimModuleSyntax`、`erasableSyntaxOnly`和`noEmit`，并把修改的产品实现与测试迁入同一类型检查。Task Verification v4不再提供通用plan/run/reconcile；Agent直接调用Project测试工具，Application只维护Project测试地图和开发完成后的Task报告。Buildr Product自身的changed planner、Browser dispatcher、Candidate DAG和资源协调继续属于Project测试实现，不进入通用Task Verification接口。

Product验证以三个正交问题解释：registry step的Static/Unit/Component/Integration/System回答证据边界，ownership与planner的affected/full回答选择范围，v3 capability的usable target与Request回答Task Delivery、Product Artifact Candidate、Published Release对象。公共`product.verification` capability通过高级provider接入唯一registry；内部DAG不进入声明或Result。Task Delivery affected/full、Product Artifact Candidate与Published Release release-only由不同Request target和selection scope决定；provider以current identity投射对应selected evidence，Quick仍只提供开发反馈，不冒充formal Task Verification；内部Task Candidate identity不等于Product Artifact Candidate。2026-08-24现场plan-only为daily-full 52 steps、1,036秒目标工作量、259秒数学下限与360秒预算；Product Artifact Candidate为66 steps、1,398秒目标工作量、349.5秒数学下限与600秒预算。下段保留的`core`、Candidate与Release执行细节均按本段公开模型解释。

Project `verification.yml` 只接受closed v4测试地图，声明稳定testing family、Project/Service scope、purpose、sourcePaths、testRoots、完整command或Agent入口、选择指导与requirements。它不登记每个测试文件，不保存一次性选择、运行或Task结果。Buildr自身registry、changed planner、Context runtime、Candidate和Release验证继续作为Product测试实现；它们由Agent按当前任务调用，不被提升为所有Project的通用Application。

Node Test Context公共源码以strict TypeScript为authority。Task read models、coordination、Daily Progress、Environment、Finish Application core与Task Development eligible case可通过统一adapter进入持久Host；已删除Execution Record专属owner和Context disposition。

执行 profile 同时拥有适用 `node-test` owner 的内部并发预算。Task Finish delivery 在 local/CI 使用两路、资源受限 CI 使用单路；静态参数与 profile 同时声明时失败关闭，避免两个配置源产生不可解释的实际并发。

Verification resource claim 必须匹配 registry 资源契约要求的 footprint、`unique-temporary-root` 隔离和 lifecycle cleanup；只使用独立临时 Git/CLI fixture 的 owner 不占用 Workspace lifecycle 压力容量。execution profile同时声明workers/processes/git/workspaceIo数值容量，DAG scheduler发放exact grant，executor与inner runner不得扩张该grant；timing evidence记录demand/grant、queue、resource wait/release和Context lifecycle。`runtime-adapter-contract` 的所有投射归属一个 run-unique 临时根，并在进程退出时只清理该根。

Product verification DAG 默认使用按近期成功样本校准的粗粒度成本调度；资源或 concurrency class 暂不可用时继续选择其他 ready step，不为高优先级 owner 空置全局容量。`critical-path` 模式使用自身成本与最长后续依赖链排序，同分时优先 fan-out producer、自身成本与 registry 声明顺序；它与`declaration`模式仅用于同一计划的受控对照。timing evidence 保留实际调度模式与每步估算优先级，不改变 owner、选择范围或证据要求。planner判定为`full`的计划在启动DAG前通过Git common-dir共享的`product-full-execution`容量一lease跨Task排队，affected与其他非full计划不占用该容量；等待期间每15秒输出心跳，最长等待30分钟，完成、失败或取消后按owner精确释放。`system-fresh-build`在独立临时Workspace内复用prepared controller，使用`workspace-saturating`与`task-lifecycle-heavy`资源而非全局`exclusive`；其余长尾 owner的调度成本同样按多次成功样本中位数粗粒度校准，成本不替代target duration或超时。

Package metadata changed planning只在拥有Git base与current内容时豁免`package.json.version`、`package-lock.json.version`和lockfile根package version；依赖、scripts、engines、其他lockfile结构、解析失败或显式paths-only调用仍保守full。registry `node-test`和受管glob在启动前解析真实文件并拒绝空集合。Release专项保持独立诊断入口，不应与普通delivery自动叠加。

生产Application与Infrastructure必须命中明确Integration/System owner。Task Execution Record专属slice已删除；Product自身changed/focus/candidate入口仍由独立测试框架持有。

目标为`dev`的PR由macOS运行主要changed/affected admission与适用Browser验证；Windows并行只消费registry step的显式`developmentRunners: [windows]`投影，空投影不安装依赖也不附加Fast admission，因此不重复执行普通跨平台owner，且不声明为完整Candidate。Formal Finish和self-bootstrap successor直接推送`dev`不自动启动GitHub Product verification，其证据分别由current Task Verification、Finish remote readback以及self-bootstrap runner的精确delta、push readback、development identity和最终Doctor持有。发布集合不再从最新`dev`冻结：`release-<version>`在精确release HEAD/tree上运行同一verification registry的分布式Candidate投影；如current main与release冲突，先由release owner在matching execution worktree进行一次main reconciliation并记录独立provenance，再递增generation、重跑完整Candidate，形成唯一tarball后经受保护release→main PR收敛。该PR固定使用merge commit，main readback必须证明两个父提交和tree equality；普通`dev`可继续前进。Selection owner为每个freeze保存不可变`freezes/<generation>`历史ref，current frozen仍阻止直接update；Candidate在任何公开mutation前失败时，修复先由基于current dev的窄support Task完成Development/Verification/Finish并交付dev，release workflow再从GitHub、tag、npm与GitHub Release证明尚未publication后允许维护者显式reopen、逐commit选择matching delivered dev commits、refreeze并对新SHA重跑完整Candidate。`release-<version>`协调Task覆盖到Candidate、main reconciliation、readiness、Publication、dev provenance reconciliation与closeout；support terminal不传播为协调Task completed。Publication成功后，Git reconciliation owner消费passed transaction evidence并只读核验current frozen selection、正式release/main refs，以及baseline和全部`sourceDevCommit`仍由current remote dev包含；结果为空effects，不要求main成为dev祖先、不写dev、reset或force push。单个macOS bootstrap使用Product锁定Node并复用checkout与依赖，先形成独立`preflight-macos` evidence，再构建唯一candidate tarball与`artifact-macos` evidence；随后并行运行macOS core、唯一消费artifact的Windows runtime/Launcher、Windows fresh build、无artifact的Windows Workspace lifecycle、Task/worktree recovery、Task Finish与Task Development四个有界shard，以及macOS/Windows最低/当前Node 24 Host Node tuple。内部shard identity与primary step集合可以随资源压力和语义owner重新分片；workflow matrix与registry必须由contract test保持一致，runner、phase、artifact依赖、Windows平台owner和稳定gate identity不变。稳定`Candidate gate`继续在macOS checkout上不安装npm依赖，直接以Node内置能力聚合closed evidence，检查source SHA、registry identity、artifact identity、primary coverage和结果完整性；branch protection不依赖内部shard名。Candidate内置`release-tarball-smoke`仍是发布冒烟owner，CI不建立重复的独立release smoke job。本地admission依赖不投射为跨shard dependency，GitHub继续由既有phase与job `needs`表达分布式短路边界。

Product `test:candidate` 额外执行 `concurrent-task-acceptance` 组合验收：在单一临时多仓 Workspace 创建两个正式Task，并发prepare两个真实Environment，从不同cwd按Task ID调用Receipt返回的绝对CLI invocation，再由测试Agent直接并发执行任务相关检查、分别记录两个current任务验证报告，并启动随机端口Preview。验收核对多仓scope、allowed roots、直接测试并发、不同Report path/digest/current applicability、错误Task无法停止对方Preview、异常子进程诊断、顺序Environment清理保持另一Task及retained doctor。该步骤使用本地临时Git与进程，不访问外部系统；Task Verification不拥有执行资源协调。

默认收尾由技能指导智能体使用既有工具与任务、环境入口。完成投影不因没有旧交接而否定任务结果；环境清理以当前身份和内容保全为准。完整职责与源码导航见 [任务收尾](../flows/task-closeout.md)。

旧收尾执行器已经退役，以下只说明保留的历史和资源能力。

历史收尾应用只提供 `task finish inspect`，保留已保存的运行、交付和诊断事实；不再提供 run、rollover、reconcile，也不写新的运行和交付终态。任务完成使用既有 `task complete`，支持 `--expected-record` 保护已观察版本。原资源维护仍核对身份、归属和内容保全；旧专业记录损坏不能撤销任务完成。父任务通过总体验收与明确授权完成；发布关联独立核对发布成果，不补旧候选或交接。完整说明见 [任务收尾](../flows/task-closeout.md)。

ready shared Environment可能没有Git provider repository set。该状态只对自动`run`保持blocked；显式`reconcile`保留Environment cleanup availability，并从current handoff scope、registry与真实Git topology重建只读delivery context，仍以逐repository remote containment作为唯一交付证明。

自动deliver在remote仍等于carrier时保持`targetDisposition: carrier`与空containment；只有remote后继可证明包含carrier时才保存`already-contained`与完整proof。Task terminal登记、Environment cleanup或carrier cleanup失败只写所属attention，不撤销已确认Delivery。

Task交付终态先于维护收敛：全部适用repository的Delivery成立后，Task Record Application尝试写`completed/noChange=false`，summary只声明任务贡献已验证交付；失败由后续`task finish reconcile`幂等修复，不重复业务push。Finish-owned carrier cleanup与Environment Cleanup分别执行并保存逐repository结果；Environment attention不得跳过carrier removal，`cleaned`只由registration/path物理读回成立，phase passed或Environment maintenance不能替代该证据。Environment Cleanup独立消费持久化Delivery evidence，为carrier、exact Task Contribution containment或no-contribution proof重新核验source与target后才删除worktree。公开compact/terminal结果正交投影Delivery、Activation、Environment Cleanup与Diagnostics。

Buildr自举Workspace的后续sync、development entry、恢复检查与最终维护登记继续由workspace-only `buildr-self-bootstrap-sync`唯一runner负责，输入可以来自自动Finish或reconciliation形成的matching delivery result。reconciliation可没有Delivery Carrier；Product self-bootstrap projector从已验证containment evidence投影activation paths，并只根据明确逐carrier cleanup evidence声明物理载体已清理。已完成的历史run可在冻结Contribution、carrier HEAD/clean状态、task trailer与remote containment全部成立时，由`reconcile`精确补做本run-owned carrier cleanup；不创建新Finish run、不重做Delivery。runner失败只形成Activation attention，不改写Task Delivery，Environment Cleanup由Task Environment独立处理。

Self-bootstrap runner仍在任何retained mutation前核验Workspace、Node、remote/branch、delivered ref、clean tree与存在的carrier inventory，并取得matching Task/run target lease；没有carrier的reconciliation结果不是错误。它只允许fetch与fast-forward，不merge、rebase、stash、reset、force push或修改foreign carrier；latest target、lease或identity不可证明时只停止Activation。

Runner用Environment retained Node显式验证retained `projects/product/buildr`的development channel、source commit、CLI entry与package/version，再尝试最终Doctor。取得matching Task/run后，passed、blocked与not-applicable都先刷新Finish maintenance terminal evidence再输出compact；maintenance writer失败保留原始主失败并标attention。入口或Doctor失败返回结构化Activation attention；新Delivery不会再进入retained Doctor blocked终态，旧doctor-blocked run只保留兼容恢复。

远端协作者提交使canonical `dev`前进、但当前会话没有与该更新匹配的Formal Finish Result/run时，Agent把它视为普通Workspace update；本地没有协作者Task是正常事实，不得从commit author、HEAD、dirty tree或Doctor drift反推self-bootstrap activation。Git transition后运行Doctor；若唯一问题是受管投影过期，则按既有授权执行Buildr workspace sync并复核Doctor，其他并存blocker保持显式。直接交付后也可凭匹配完成任务和可核验 Git 输入进入`buildr-self-bootstrap-sync`；旧模式只有matching Finish才进入，普通sync不创建Task、Finish或新的lifecycle authority。

Development Launcher安装前，self-bootstrap runner的continuity helper只读取Development Web Data Root并用instance secret认证health；只有健康development实例才冻结当前端口与旧PID，绝不读取、停止或清理released实例。manager原子更新Launcher后，helper通过retained `projects/product/buildr web --port 4458 --no-open`、Environment retained Node与新Launcher identity恢复服务，并记录previous/current端口、新旧PID及retained source/HEAD/Node；历史随机端口实例因此迁移到`4458`。原本未运行、stale或其他channel保持按需启动。恢复失败只回收本次启动且PID可证明的进程，保留代码交付与新Launcher，并以自举激活注意（Activation Attention）停止后续gate/finalize，不回滚交付、不停止foreign占用者。

同一carrier根存在多个run时，runner只隔离可证明ownership、路径和identity的foreign carrier；不得修改或删除仍代表repository carrier的内容。历史Result已由Product明确声明全部carrier为`cleaned/root: null`时，runner只有在精确run container真实、非symlink、未越界且完全为空的情况下，才以`stale-empty-container`兼容分类执行非递归空目录删除；非空、identity不匹配或删除失败继续阻断且不递归清理。已交付但cleanup pending的owner仍由Agent通过Task Environment独立处理，不成为其他Delivery或Activation的全局前置条件。

多个自动Finish run只在相同repository-scoped target mutation窗口竞争短lease；多repository不构成跨remote原子事务。逐repository push/readback后立即checkpoint，部分成功必须保留，恢复或reconciliation不重复push已交付repository。

retained canonical Workspace存在metadata-only或其他自动adapter不适用情形时，Agent仍可选择精确Git Operations或PR交付；每次operation保持独立Result，完成后由reconciliation登记Task Delivery。无法证明repository/ref/scope或完整push range时停止，不使用`git add -A`、stash、reset、隐式rebase/merge或force push。

Task Environment 候选集成后，主 Workspace runtime 仍从 retained checkout sync/doctor；未合并 task checkout 不更新主 runtime。sync 只维护当前 SQLite authority 与 runtime assets，不扫描旧 Task Environment receipt 或 `.buildr/tasks/`；Git provider evidence 继续由窄 provider authority 维护。


## 验证与交付内容完整性

Product testing以测试侧`HTTP_CONTRACT_FRESH_BUILD_FAMILIES`统一登记HTTP DTO generator、服务端Schema、Buildr DTO与Buildr Web DTO；Contract owner在现有Fast admission中校验family、文件与两端输出闭合，Fresh Build System fixture消费同一inventory并继续以真实`npm ci`和`build:web`持有System evidence。该inventory只属于Product test tooling，不进入npm runtime、Project verification declaration、Task Result或跨invocation cache；Fast静态通过不冒充Fresh Build System证据。

Agent-reviewed Delivery Adaptation在采用carrier前按冻结Task Contribution逐路径闭合：目标tree精确包含、carrier相对Delivery Baseline实际改变，或Agent通过matching run/resume为该repository/path提供非空理由并明确承担语义判断。Buildr只证明路径集合、Git bytes、run/target/carrier identity与coverage identity，不把Agent判断描述为机器语义证明；未知、重复、遗漏或漂移使同一run保持blocked。coverage进入既有carrier/delivery/cleanup proof及compact计数，不新增SQLite表、第二Result或事件历史。自动run受阻时Agent仍可修改run-owned carrier、改走PR或直接Git，随后由Delivery Reconciliation从真实remote重建交付证据。

## 与 buildr-web 的交接

- `buildr-web` 拥有前端源码；`npm run build:web` / `dev:web` 委托 sibling `../buildr-web`。
- `buildr/package.json`中的`build:web`委托和Task Verification所选前端能力，是Agent判断当前Task需要同时准备`buildr`与`buildr-web`的正式源码事实；Agent把该判断登记到Task专属Environment Plan，静态Project配置不充当routing authority。
- 正式静态资产落在本 Service 的`web-dist`；公共application payload只消费已提交dist，npm package不携带`buildr-web`源码或Vite toolchain。
- Buildr Web Runtime 继续从 `web-dist` 同源 loopback 托管并注入 session，不改为独立前端端口。

### Changed selection 解释与现场结论

- Changed plan的`selectionAudit`复用同一planner结果，逐step区分direct owner、Full展开、profile/admission和dependency closure，并投影触发path、执行边界、primary evidence owner、公共结果与目标预算。
- Full pattern、稳定reason code和用户说明只由ownership authority声明；planner、registry、ownership、scheduler/executor、验证入口和执行基础变化分别以稳定authority reason升级daily-full。未知高风险production path缺少领域owner时阻断，不由通用Unit或CLI architecture静默兜底。
- 2026-08-24三个近期`product.delivery`可回放样本中，2个保持affected、1个因registry execution graph变更合法Full；没有观察到普通Task无理由Full，剩余瓶颈是被正确选择的真实primary owner。本结论来自小样本，不外推长期升级率。

## 局部术语

本 Service 当前不重定义 Project glossary。CLI、runtime adapter、Component、provider、consumer 和 binding 继续使用 [Project canonical terminology](../glossary.md) 及相关 specs 的精确定义。


Browser selector dispatcher通过异步owned phase runner记录web-dist、fixture、browser、assertions与cleanup，并以外层capability deadline兜底回收owned descendants。`concurrent-task-acceptance`真实声明workspace-saturating、task-lifecycle-heavy与app-runtime，Preview启动基于instance/health/resource readiness的有界等待；Full capacity仍为1，资源声明只负责压力节流。
Task Development 的current-input discovery是面向`observe|policy`的response-only `buildr.task-development-current-input/v1`投影；它复用current Task/Environment/Receipt与v4测试地图事实，mutation Application仍是唯一writer并重新校验漂移。兼容policy只服务旧研发记录；Agent直接选择和执行开发反馈测试，开发完成后Task Verification只保存一个有意义的报告。
