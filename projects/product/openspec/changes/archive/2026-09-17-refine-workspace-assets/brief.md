# 工作空间对象管理改进

解决项目导航、对象注销、真实仓库引用和列表延迟。依据工作空间应用层、三个声明清单与用户确认方案。

当前知识影响评估（assess）：更新现有对象关系说明及代码地图中受影响入口；技术图未改变项目与服务基数，无需重绘。实现后按实际接口与迁移行为核对（reconcile）。

当前知识核对（reconcile）：已更新 `knowledge/code-map/system-services-assets.md` 和 `knowledge/docs/architecture/service-architecture.md`；资产维护指引补充真实根目录、删除和规范化命令。图中的项目与服务引用基数不变。诊断（Doctor）仍主动检查真实 Git 状态，按仓库去重；轻量列表不承担此检查。

验证：完整低成本回归通过（216 项单元、202 项契约、5 项组件及类型/规范检查）；19 项关系集成测试、22 项兼容性测试通过；正式构建及全部 16 项浏览器验收通过，浏览器错误为零。集鲜 21 个仓库只读测量，新服务端列表处理为 12.5–41.1 毫秒，旧 Git 扫描为 2.58–3.34 秒；该数字不包含网络和页面渲染。当前运行版及实际业务清单未更新。
