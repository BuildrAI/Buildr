# 放宽工作空间主内容区域

用户已确认 `docs/prototypes/main-width.html` 中的铺满方案并授权实现。保留完整导航与现有目录交互，独占桌面留白 32 像素，详情并排留白 24 像素；正文、表单继续使用阅读宽度。

当前认知影响：`knowledge/code-map/system-services-assets.md` 的共同组件职责仍成立，技术图和术语无变化。`knowledge/docs/services/buildr-web.md` 中统一限宽和默认 1440 像素的说明已与当前实现存在漂移，本次资源目录新增铺满例外；该文档维护不在已授权范围，保留具体校准建议，不阻止本次实现。

验收：正式构建通过；生产托管页面中检查宽屏目录边距、并排关闭恢复、窄屏不越界、搜索不重置、表单与阅读区域仍限宽。

实现复核：正式目录样式已按上述范围完成，共同组件职责、技术图和术语无变化。正文页面实测仍为 1200 像素上限；2560、1440、900、390 像素窗口下目录留白及溢出检查通过。现有知识说明的校准建议仍保留，未修改其正文。
