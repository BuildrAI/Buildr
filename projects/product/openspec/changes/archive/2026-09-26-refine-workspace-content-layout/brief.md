# 页面宽度与布局优化

用户已确认上一轮审查建议并授权优化。复用真实界面和主题，先提供隔离生产预览。

## 当前知识影响

- knowledge/docs/services/buildr-web.md：既有统一限宽说明需要后续校准；本次仅授权界面优化，保留建议，不扩展知识文档写入。
- knowledge/code-map/system-services-assets.md：模块职责不变，aligned。
- 技术图与术语：无架构或长期术语变化，not-applicable。

## 实现与审看

- 列表、工作空间总览、项目主页、知识外框与工作台采用可用区域宽度；知识及任务正文上限 880 像素，图表和报告可伸展。
- 文章列表按自身容器宽度合并元信息，保留项目链接、状态、日期及操作。组成在窄容器中纵向展示去重对象及真实引用。
- 已重建 `docs/prototypes/main-width.html`；总览同源原型的新版本为 `docs/prototypes/workspace-overview.html`，保留原历史归档不改写。
- 正式构建、98 项前端逻辑检查、9 项项目相关浏览器检查通过。2544、1440、900、390 像素下六类页面无横向溢出；知识正文及展开任务正文均为 880 像素以内。两份原型的宽窄布局、宽度比较、搜索和导航已验证。
- 审看阶段提供了独立演示数据上的候选正式页面；用户随后授权收尾，按当前成果执行交付。

## 回归限制

候选全量浏览器回归为 37 通过、13 失败（含父项及后续连带失败）。首批为目录登记旧选择器和工作台摘要、资料、刷新断言，随后出现 `Route is already handled` 导致后续页面关闭。日志：`/tmp/layout-browser.log`。

在未改动的 `a633d571` 上运行相同全量入口，目录登记、工作摘要、资料与刷新等相同测试也失败，且最终达到 360 秒外层超时（退出码 124）；这证明这些失败已存在于基线，不构成全量通过证据，也不据此证明所有交互均无问题。日志：`/tmp/layout-baseline-browser.log`。候选最终项目专项 9 项全通过，另有四尺寸实测作为本轮布局证据；任务验证总体如实保留为未通过。
