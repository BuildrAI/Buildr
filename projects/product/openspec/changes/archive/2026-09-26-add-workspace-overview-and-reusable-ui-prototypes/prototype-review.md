# 第一阶段原型审阅

2026-09-25。第一阶段已实现共享界面基础、两项技能（Skill）和两种原型阅读入口；工作空间总览仍采用模拟数据（Mock Data）。下一步按本次反馈完善同一源码，再接入真实登记与导航。本材料不是整项任务完成报告。

## 用户否定后的自审与修正

前一版被用户明确否定，不能沿用“原型体验已验证”的结论。问题根因是只追求少数组件（Component）的源码复用，却重画了产品外壳并丢失上一版探索交互；整体缩放又使阅读与鼠标目标变小，原有检查未覆盖真实使用路径。

本轮恢复真实产品的顶部主导航、完整侧栏与目录列表，均使用共同源码；取消整页缩放，按真实宽度响应。总览补回搜索、无结果恢复、项目聚焦与取消、悬停关联高亮。图中项目直接进入“项目组成”，普通列表进入概览；说明同步修订。实际鼠标验证已覆盖这些路径和服务详情打开。

本轮重新通过前端正式构建、93 项前端逻辑检查、20 项受影响集成及资源检查、8 项真实项目浏览器回归。上轮服务知识滚动恢复与通用任务样例问题仍未修复，不声称整项已完成。

## 审看入口

本机隔离预览：[完整三栏阅读](http://127.0.0.1:52014/workspaces/ed5d5205-a3fe-4320-8f20-c156586edfa4/tasks/overview-review/prototypes)。使用本轮生产构建与隔离工作空间，不是已安装的 Buildr。服务停止后该地址失效；可保留、重建的原型文件见 [工作空间总览](prototypes/workspace-overview.html)。单独打开此 HTML 可体验画面；任务限定的三栏阅读由 Buildr Web 提供。

| 关键页面 | 本次重点 | 已实际检查 |
| --- | --- | --- |
| 工作空间总览 | 项目、服务、代码位置及共享引用 | 页面选择、独立对象、空白状态、局部失败提示与键盘重试 |
| 项目概览 | 保留用途、工作入口、知识与服务列表 | 完整页面呈现，与共同源码的项目标题和导航入口一致 |
| 项目组成 | 本项目引用的实现组成 | 页面切换和对应说明；真实读取尚未接入 |
| 关联已有服务 | 复用实际单选即保存交互 | 过滤与新增入口、成功后列表更新、失败时保持已有引用 |
| 对象详情与分屏 | 服务和代码库完整对象阅读 | 不同类型保留页签、代码库打开、展开与恢复、文档入口 |

说明与实施清单共用阅读容器：任务内默认收起、独立宽屏默认固定；已检查悬停、离开延迟、固定、切页、关闭、键盘退出、焦点返回、窄屏固定限制及两个面板互斥。任务左菜单直接列出五个页面，“单独查看”打开完整三栏新页签。旧格式文件、刷新后选中项消失的回退提示都已在两种入口验证。

## 共同源码与模拟边界

路径相对于 `services/buildr-web/`：

- `src/features/project/components/ProjectServicesView.tsx`：真实和模拟入口共用关联选择器、列表及失败反馈。正式入口保留版本校验，模拟入口只修改内存。
- `src/features/project/components/ProjectHomeHeader.tsx`、`ProjectHomeEntries.tsx`：共用项目标题、工作与资料入口。
- `src/features/workspace/components/AssetHomeView.tsx`、`AssetDocumentList.tsx`：共用对象页结构与文档入口；真实请求和编辑仍由正式入口承担。
- `src/components/WorkspaceStage.tsx`：继续使用已有分屏与对象页签。`SideReadingPanel.tsx`、`useSideReading.ts` 供实施清单和功能说明共用。
- `src/features/workspace/components/WorkspaceComposition.tsx`：候选总览展示组件（Component），当前仅接入模拟入口；下一阶段在同一来源接入真实登记。
- `src/prototypes/workspace-overview/main.tsx`：模拟入口装配。顶部栏和侧栏框架改为共用 `AppShellView.tsx`，导航项共用 `AppNavigationItem.tsx`，目录列表共用 `ResourceDirectory.tsx`；新增与编辑表单、工作台跳转、真实代码状态不在本次原型中实现，不声称所有内容都来自生产页面。

原型文件 1,147,836 字节，低于现有 2 MiB 限制。内嵌五页说明、30 项本地来源摘要以及观察基线 `e5cd2fa268967bd81fa1841e3a3e07ccc494216a`；来源摘要记录未提交文件内容。资源和脚本内联，浏览器观察的画面资源请求为空；画面仍使用 `sandbox="allow-scripts"` 与既有内容安全策略（CSP），没有真实业务写入。

## 技能授权边界

前端开发技能（Frontend Development Skill）持续指导组件（Component）、主题和交互复用，但不自动启动原型。新增关键页面或主要交互变化且存在设计选择时，智能体（Agent）先询问是否需要原型；明确同意才使用原型技能（UI Prototype Skill）。普通小改动直接实施；同一任务已有授权或拒绝继续有效。本任务已有授权，默认只覆盖关键页面及必要状态。

## 验证与尚未完成

- 前端正式构建通过；前端逻辑检查 93/93 通过。
- 后端低成本完整检查已通过；最终相关元信息、技能资源、旧文件与任务读取集成等专项检查 49/49 通过。
- 项目浏览器回归 8 项通过；共享选择器真实保存、创建与冲突反馈保持可用。
- 两项技能（Skill）的格式校验通过；OpenSpec 严格校验通过，收敛预检为 `ready`；知识索引及新增来源路径校验通过，验证声明校验为 `ready`。
- 通用任务浏览器回归未通过：样例任务已有子任务而断言仍期待普通任务工作路径，未覆盖到本次阅读流程。本次已在独立普通任务补查新入口与清单交互，但不等于整套任务回归通过。
- 服务浏览器回归两次在知识目录返回后的滚动位置恢复检查超时（`service-knowledge-journey.ts:172`）；同轮其他十项检查通过。失败原因尚未确认，保留为下一阶段需定位的回归问题，不宣称已排除本次改动影响。

总览真实数据、完整关联更新、最终主流程回归、正式交付与自举激活均未完成。没有发布或更新已安装应用。

## 重建与接续

在前端服务目录安装现有依赖后，用项目要求的 Node.js 执行：

```sh
node tools/build-prototype.mjs ../../openspec/changes/add-workspace-overview-and-reusable-ui-prototypes/prototypes/workspace-overview.html
```

准备依赖沿用 `preparation.yml` 的两个服务安装入口，不新增依赖或运行时。`verification.yml` 已纳入前端构建工具路径与原型的独立检查边界；普通浏览器回归不会自动生成原型，也不能替代逐页验证。

受影响的任务说明与代码地图（Code Map）及阅读索引已更新。技能投射机制和项目、服务、代码库关系语义未变，对应两篇架构说明已复核，无需改写。

## 功能说明与技能组件修订

右侧统一为“功能说明”，综合提案与设计，只解释当前画面上与任务有关的功能和操作结果；已去除沿用组件、上一版差异等制作说明，不要求所有需求对应原型。卡片悬停或聚焦高亮区域，移开清除，点击定位，不绘制说明连线。

通用组件（Component）已放入 `services/buildr/resources/workspace/skills/buildr/ui-prototype/assets/feature-notes.js` 与配套样式，并登记技能分发清单；接入示例见该技能的 `references/feature-notes.md`。使用原生浏览器能力，不依赖特定框架。Buildr Web 与本次原型导入同一来源，React 仅适配生命周期。组件随构建内联，后续原型只需提供本次任务说明、区域标记和本地画面切换。尚未更新已安装技能投射。

实际操作发现并修复了两处同步问题：原型内切页期间宿主回传中间状态导致模拟关联被重置；初始装载覆盖先到的悬停高亮。直接打开对象详情与关闭最后一个详情均保持画面、目录和说明一致。另补用了真实 `PageTabStrip.tsx`，恢复页面页签的布局空间，解决宽屏对象关闭入口被顶部栏遮挡。

本轮最终前端正式构建通过，前端逻辑检查 95/95；资源清单、技能运行兼容与相关集成检查 53/53。浏览器在 1440 × 1000 和 1600 × 1000 实际验证卡片悬停、移开、键盘聚焦、点击定位、关闭详情后的同步、保留搜索输入与关联结果、切换到项目组成。早期检查因临时说明面板覆盖对象按钮及遗漏页签空间而中断，按实际原因处理后重走关键路径通过。既有两项回归缺口仍保留，不据此宣称整项任务完成。

## 正式功能接入后的当前状态

用户确认实施后，真实总览与项目组成已接入；上述原型阶段缺口已定位并完成相关回归。当前结果以 [本轮实现与验收](implementation-review.md) 为准，保留本文的历史观察。当前原型重建为 1,149,305 字节、31 项来源摘要。
