## 1. 建立真实页面与模拟入口的复用基础

- [x] 1.1 为关联服务分离必要的数据与操作接入，正式与模拟入口导入同一选择器和展示源码；通过导入关系和交互检查证明选中即提交、过滤及失败反馈一致，模拟入口无真实请求。
- [x] 1.2 从实施清单提取通用侧边阅读容器与交互，分别装配原内容和功能说明；验证悬停、离开延迟、点击、固定、键盘退出、焦点返回及窄空间行为，原实施清单无回归。
- [x] 1.3 建立最小独立预览构建及固定样例，生成自包含 HTML；验证当前文件大小限制、离线资源、内联脚本执行、主题一致性和内容安全策略（CSP）兼容，记录来源观察与未覆盖项。

## 2. 沉淀前端与原型工作方法

- [x] 2.1 新增 `frontend-development` 技能（Skill）及必要示例指引，覆盖先调查、复用、职责拆分、样式来源、模拟操作和变更影响；用本次两个入口实例逐项核对，确认不要求全量重构或强制框架。
- [x] 2.2 更新 `ui-prototype` 技能（Skill），明确关键页面默认范围、源码复用、候选构建、逐页说明、快照来源及验证；同步修订与原禁止编码式原型不兼容的检查，保留明确请求、非正式规范及模拟边界。
- [x] 2.3 在唯一资源清单登记新增技能（Skill）及实际辅助文件，补充适用资源检查；验证可选安装、同名重载、卸载和投射方式兼容，无新能力绑定或任务状态。

## 3. 完成任务内与独立阅读

- [x] 3.1 扩展原型文件内可选元信息的有界读取，提供关键页面、状态和纯文本说明；以旧文件、合法新文件、超限和非法输入验证兼容及局部诊断，不扩大路径读取权限。
- [x] 3.2 将关键页面直接纳入方案设计左侧菜单，移除主内容重复列表；验证页面切换、刷新后选中项失效及多关联变更的标题和身份不混淆。
- [x] 3.3 接入共用侧边阅读能力，任务内默认收起说明、独立阅读宽屏默认固定；验证切页保持固定选择，实施清单和说明不叠加面板。
- [x] 3.4 增加独立三栏阅读路由和“单独查看”，保留任务限定与有效页面状态，删除原型工具条的扩大阅读及手动缩放；验证新页签有完整列表、画面、说明，长内容和窄屏可读。
- [x] 3.5 实现最小阅读消息协议及双向位置提示；验证合法状态同步，错误来源、旧代次、未知标识和恶意消息不能执行真实操作或扩大读取范围，内容仍在既有安全沙箱（Sandbox）内运行。

## 4. 用共同源码形成修订后的关键页面

- [x] 4.1 构建总览及项目组成的候选展示组件（Component），沿用现有主题、导航和完整对象页；生成五组关键画面并确认两种入口导入共同源码，不复制既有交互。
- [x] 4.2 纠正旧演示中的关联服务、独立阅读、说明方式及名称，使用真实单选即关联语义；逐页浏览检查画面、操作结果、说明和任务范围一致，小改动不增加独立页面。
- [x] 4.3 提供修订后的可审看成果及逐页验证摘要；按用户反馈维护受影响源码和成果，明确确认结果，再接续真实数据接入。

## 5. 接入简洁工作空间总览

- [x] 5.1 增加工作空间“总览”入口与项目“项目组成”查看方式；验证不覆盖工作台 `/overview`，原列表和项目概览仍可进入。
- [x] 5.2 接入现有登记读取和稳定身份，展示用途、组成与共享引用；以共享服务、独立服务、共用代码库和空白样例验证去重与引用正确，确认无全量 Git 状态扫描。
- [x] 5.3 复用既有对象打开与副屏能力，接入原关联保存和更新通知；验证总览进入项目组成、列表进入概览、同类替换、异类并存、左右独立阅读及现有分屏尺寸规则。
- [x] 5.4 补齐局部失败、重试和关联冲突的最小反馈；验证未知不记零、已知引用保留、陈旧写入不覆盖、成功后相关展示更新。

## 6. 按最终影响校准与验证

- [x] 6.1 按最终实现校准 `knowledge/docs/architecture/task-system.md`、`knowledge/code-map/task-system.md` 和 `knowledge/code-map/technical-layers.md` 的阅读、源码职责与原型发现说明；检查所有新增引用路径可解析且描述与实际一致。
- [x] 6.2 核对 `knowledge/docs/architecture/buildr-skill-system.md` 和 `knowledge/docs/architecture/project-service-repositories.md`；机制和关系语义未变时记录无需修改，只有真实受影响部分才更新，不添加平行事实库。
- [x] 6.3 核对本次构建与检查入口在 `preparation.yml`、`verification.yml` 中的适用声明，按实际变化维护；验证新入口的依赖、隔离输出和证明范围清楚。
- [x] 6.4 执行匹配改动的前端构建、组件行为、原型发现及安全隔离检查，并通过项目规定的隔离生产托管进行浏览器验收；验证正式和模拟两种入口、旧原型兼容、独立三栏阅读及总览主流程，报告实际未覆盖范围。

## 第一阶段接续记录

原型阶段的历史观察见 [原型审阅说明](prototype-review.md)。用户已明确要求“按这个方案实现功能”，真实总览与项目组成、共用原型阅读和技能资源已完成，6.4 所列相关检查已通过。当前实现、回归修正及运行边界见 [本轮实现与验收](implementation-review.md)。

## 本轮功能说明调整
- [x] 将侧栏统一为任务相关功能说明，提取卡片组件，验证悬停、聚焦、定位及画面同步，不重置操作数据。
- [x] 将功能说明组件随原型技能分发，当前阅读器与原型实际复用；检查安装清单、可移植构建及使用示例。
