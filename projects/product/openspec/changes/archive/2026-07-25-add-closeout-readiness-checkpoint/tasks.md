## 1. 收尾就绪检查

- [x] 1.1 在 `task-finish` 增加有范围的 Candidate 前 readiness checkpoint 和外部 CLI 授权边界。
- [x] 1.2 在 OpenSpec archive 流程增加空 active-change scaffold 的证据化检查与最小清理边界。

## 2. Package 与验证

- [x] 2.1 同步随包的 `task-finish` target，并增加 contract assertions 覆盖触发、阻塞和不扩张 capability contract 的边界。
- [x] 2.2 运行受影响的 contract、package/static、OpenSpec strict 和 workspace doctor 验证。
