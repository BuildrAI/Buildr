# 实现与验证结果

2026-09-18，任务分支 `codex/pilot-skill-projection-knowledge`，实现基线 `16ce3c36`。成果尚未合入、归档或发布；本报告不代表正式自举环境已更新。

## 已实现

- 项目与服务知识入口；解释文档直接嵌入同一成果的图与地图，保留独立查看、返回及阅读位置。
- 知识模块（Knowledge）按登记范围解析局部索引，提供对象、成果与来源只读接口（API）；实际摘要、缺失与不可读逐项呈现。
- 文档链接定位真实文件；技能（Skill）使用既有资源副屏与 `SkillsPage` / `SkillHome`，保留主文档参数；源码有行号、必要定位与本地轻量高亮。
- 图示使用现有隔离 HTML 响应策略；图源和展示分别记录摘要，旧图保持阅读，已登记对象可从图旁入口进入。只有主动发送已关联对象标识的图示才使用节点消息，未猜测旧图节点身份。
- 修改意见使用既有动作抽屉，带范围、对象、索引与来源观察、所选来源；只形成可复制指令，不自动发送或执行。
- 随包 `code-map`、知识维护与普通开发的条件化交接；术语沿用项目唯一术语表，新增知识阅读关联与来源观察定义。

## 实际检查

| 范围 | 结果 |
| --- | --- |
| 后端完整低成本回归 | `tools/development/run-development-npm run test:fast` 通过，包含类型、单元、组件、契约、结构与相关静态检查 |
| 相关真实文件、关系与装配回归 | `knowledge-query.test.ts`、`asset-relationships.test.ts`、`bootstrap-module-architecture.test.ts` 合计 49 项通过 |
| 前端 | 正式构建通过；19 项测试通过，包含知识路径、路由与隔离消息反例 |
| 随包资产 | `package check` 通过；新的代码地图技能（Skill）在隔离工作空间实际安装及读取，附属资料入口可读 |
| 规范 | OpenSpec 严格校验通过，相关冲突预检 `ready` |
| 资料引用 | 新专题与地图等 28 个原生相对链接均指向存在的文件 |
| 浏览器 | Codex 内置浏览器检查生产托管构建，项目→知识→解释文档→内嵌/独立成果→技能及源码副屏→修改意见；服务未建设提示与项目返回路径通过 |

初次回归发现新模块未加入装配快照及允许的具名模块依赖清单，修订后完整低成本回归通过。联调发现技能详情附加字段需投影为窄响应、知识深链接需被 HTML 宿主接收、副屏需保留主文档参数，均已修正并验证。

浏览器核对 1440 桌面宽度的文档与副屏无页面横向溢出。职责图经 Archify `deliver` 的 9 项检查通过，组合错误与警告为 0；1440×900、1600×1000、1920×1080、2048×1320 的页面布局指标均无横纵溢出，实际查看小、大尺寸图示。使用浏览器工具取得视觉证据，没有把自动指标当作人工语义验收。

图源摘要：`4a9aae9f43cc4789f54b32d9c69f955204b2bc7a43d54ac25634f9a33a027a2f`；原展示摘要：`5ad54afdaedef0177776cb708cce20caac74d4fbf21cddbb852fb08f65f13bcc`。网页响应仅移除远程字体链接并实施隔离策略，源展示不被改写。

## 真实维护样例

样例位于隔离工作空间的 `knowledge-sample` 项目，通过同一生产接口（API）与网页阅读。下列各步实际修改文件；页面刷新分别观察一致、待核对、恢复、缺失和替代来源。无语义影响步骤仅添加注释，核对后更新观察，地图和说明保持原字节。

| 步骤 | 索引摘要 | 地图摘要 | 说明摘要 |
| --- | --- | --- | --- |
| initial | `73683adb9f61` | `b3ccc9ae2366` | `c1a47c424f28` |
| modify | `73683adb9f61` | `b3ccc9ae2366` | `c1a47c424f28` |
| maintain | `4063b95f7732` | `04dd264cd3ed` | `c1a47c424f28` |
| delete | `4063b95f7732` | `04dd264cd3ed` | `c1a47c424f28` |
| replace | `ab52e3f85487` | `8bf84d861aa4` | `c1a47c424f28` |
| no-impact | `551c0a0cd48f` | `8bf84d861aa4` | `c1a47c424f28` |

维护结果区分：来源修改与替代后的地图为实际更新；注释反例为核对后保留。范围外缺口、规范冲突与无需调用术语治理等条件做了静态走查：范围外只提建议，冲突交还事实源，不为普通开发强制新建规范变更。静态走查不证明任意模型一定执行指引。

## 边界

- 验证对象为 macOS 本机隔离运行的真实构建，不是旧静态原型；未宣称全部平台、任意项目或所有无障碍场景已覆盖。
- 首个完整对象为知识建设与维护。其他已有图仅收录阅读；项目级全景代码地图仍按用户决定暂缓。
- 维护由智能体（Agent）在授权开发中完成，网页刷新只读观察；没有后台语义自动改写器。
- 隔离样例的可选 Archify 组件文件已安装并可读；安装命令随后执行的完整 Doctor 报告该临时工作空间声明未完全就绪。此结果未作为正式自举或环境健康通过，也不否定已独立验证的读取与安装文件事实。
- 用户对正式实现的使用反馈仍待收集；不以开发测试代替人的最终体验判断。


## 2026-09-19：用户引导的架构知识建设

本轮补充 3 项已完成，累计 20 项。沿用同一任务工作树（Worktree）和真实预览运行器；未提交、合入、归档或发布，未激活正式自举环境。

### 已实现与实际走查

- 知识首页及未建设服务显示“构建架构知识”，主题必填，关注范围与阅读问题可选；当前登记身份、真实代码范围、已有文章与观察随指令保留。
- 文章提供“完善这篇说明”，优先修改当前唯一正文；跨文章链接切换到目标主题，重新加载它自己的内嵌成果。源码副屏保留主文档。
- 页面仅准备、复制指令，状态明确“尚未执行”。在真实浏览器填写主题并生成/复制后，核对索引摘要仍为 `58389aae0d2d805b286a61633f4a56dcbfdd817cca860189a7f2bf78aac8a922`，新文章尚不存在；没有假完成状态或自动占位。
- 随后由智能体（Agent）实际调查对象关系与代码库规范、领域校验、应用、持久化和页面，实现[项目、服务与代码库如何协作](../../../../knowledge/docs/architecture/project-service-repositories.md)、对应地图与关系图。文章区分当前事实、设计依据、建议和未覆盖范围，没有改变登记或业务代码使文档成立。
- 刷新同一知识页后找到第二个主题，文章实际嵌入关系图和地图；点击代码链接读取真实文件，点击完善后保留主题、既有正文路径、范围与来源观察。未建设的 Buildr 后端服务也能打开表单，范围正确标明当前服务。
- 关系图是共享引用和代码定位的模型示意，明确不表示实际登记数量、运行调用或部署拓扑。

### 检查与修复

| 检查 | 本轮证据 |
| --- | --- |
| 后端完整低成本回归 | `test:fast` 通过；日志 `/tmp/knowledge-construction-fast.log` |
| 前端 | 正式构建通过，23 项测试通过，包含建设指令身份与输入、已有正文复用、跨文章定位；日志 `/tmp/knowledge-creation-unit-final.log` 与 `/tmp/knowledge-creation-build-final.log` |
| 随包资产 | 检查 13 个包含项与 311 个文件通过；知识建设指引在隔离工作空间实际恢复并可读 |
| 规范与冲突 | 严格校验通过，冲突预检 ready；保留其他活动变更 |
| 实际引用 | 新文章和地图的 22 条原生链接指向存在的文件 |
| 图示 | Archify 9/9 检查通过，0 错误、0 警告；1440×900、1600×1000、1920×1080、2048×1320 页面指标无溢出，实际浏览独立与文内图示 |

接入第二个主题时，发现序列化器为重复列表生成 YAML 别名，而当前读取器拒绝别名；改为显式列表后，真实接口与页面成功加载两个主题。代码地图制作指引补充这一现有格式要求。文内图示采用已有嵌入模式，修复窄阅读区域因独立查看布局造成的裁切；独立查看仍保留完整控件。复制失败时明确允许手动复制，不假报成功。

当前关系图的图源摘要为 `1947643adac585f601248ea6de976cd091f86e104fb64ddae7163bc09c8fd3b7`，展示摘要为 `cd8f79124910b3a23df844619d9fa201a480bcec056a979b9b8a6bb26a8cbaa0`。来源观察仅在本次实际核对后更新，摘要一致不作为语义正确的替代证明。

本轮证明了人在网页提出阅读目标、智能体（Agent）接续建设、成果回到同一阅读入口的路径。没有实现网页直接调度智能体、后台自动发现主题或语义自动改写；生成指令本身不是执行。正式用户体验仍需本人试用，不能由检查数量代替。


## 2026-09-19：阅读体验与代码审查修订

用户对八项体验和方法问题提出改进，本轮新增 5 项完成，累计 25 项。具体审查结论见[当前实现审查与改进](review-2026-09-19.md)。仍在原任务工作树（Worktree），未合入、归档或发布。

### 实际结果

- 项目主页采用完整可点击知识入口，项目详情与知识目录共享同一主标签；原有重复标签恢复时去重。面包屑表达阅读层级，切换主标签保留知识分类及检索条件。
- 架构知识、技术图与代码地图使用同一检索框和结果样式。检索当前索引标题、主题说明与路径，支持空结果和清除；专题地图可读，全景地图明确未建设。
- 技术图可以独立发起建设。实际填写主题并核对生成内容以图源、展示、事实依据为结果，不强制新建文章；提示尚未执行。
- 文中与目录中的图和地图均在副屏独立阅读。浏览器实际打开地图，再点源码、关闭源码返回地图，并以中文技能名称打开原有技能详情；主文档与当前对象保留。直接打开关系图时，主目录仍显示技术图分类，副屏有真实图示。
- 地图将规范、协议入口、应用、领域规则、文件数据访问及技术支撑连成说明，采用可点击文件树；路径相对项目根但只展开主题范围。技能职责以人可理解的名称呈现，来源完整观察默认折叠，变化与缺失仍主动提示。
- 前端把目录、成果呈现、异步读取和纯筛选分开。公共 Markdown 渲染器补齐原生嵌套列表；联调中曾观察到旧渲染器把目录压为段落，修复后实际页面出现可点击层级。后端协议不重复传输图示 HTML 和图源正文，独立图示读取及版本保护保留。
- 两篇文章、两张地图及相关索引已更新；具体地图方法归 `code-map`，整体文章组织与阅读验收归 `current-knowledge-maintenance`。未改变既有能力绑定和 v3 约定。

### 本轮检查

| 检查 | 结果与证据 |
| --- | --- |
| 后端完整低成本回归 | 通过，`/tmp/knowledge-review-fast.log`；覆盖类型、单元、组件、契约及相关静态边界 |
| 知识读取、实际 HTTP 与模块装配 | 21 项通过，`/tmp/knowledge-review-integration.log`；包含 JSON 不带图示正文、独立图仍可读、陈旧版本拒绝与沙箱策略 |
| 文档渲染 | 11 项通过，`/tmp/knowledge-review-markdown.log`；包含真实父子层级、原生链接、混合列表和脚本转义反例 |
| 前端 | 26 项测试及正式构建通过，`/tmp/knowledge-review-web-tests.log`、`/tmp/knowledge-review-build.log`；包括分类检索、独立图指令及同项目标签恢复 |
| 随包资产 | 13 个包含项、311 个文件检查通过，`/tmp/knowledge-review-package.log`；两个方法在隔离试用工作空间恢复成功，正式自举未激活 |
| 规范与冲突 | 严格校验通过，冲突预检 ready，`/tmp/knowledge-review-preflight.json` |
| 引用 | 两篇文章和两张地图的 65 条原生引用对应存在的文件或实际技能映射，均有阅读关联 |
| 浏览器 | 生产托管真实构建，主入口、三类检索、空结果清除、分类恢复、图与地图副屏、文件与技能详情均实际操作；1600 宽度两侧各 668 像素，页面与两侧无横向溢出；窄窗口沿用既有覆盖式副屏 |

本轮图源和图示语义未改，复用此前图示检查；改动只涉及嵌入与副屏呈现并实际查看。构建的 Ant Design 公共块大小出现建议性告警，构建成功，未新增依赖。性能改进仅证明响应不再重复携带正文，不宣称完成整体性能基准。

验证不包含跨平台完整测试、全文检索、全景地图或网页直接调度智能体（Agent）；这些不在本轮承诺内。人的理解效果仍需用户试用确认。


## 2026-09-19：事实文件树与完整详情阅读

本轮 5 项完成，累计 30 项；仍在原任务工作树（Worktree），没有合入、归档或发布。

### 实际阅读结果

- 两张地图的事实树包含规范及真实前后端文件；具体成果树包含文章、地图、图源、HTML 展示及存在的依据文件。83 条原生引用均存在且有阅读关联。
- 目录按作者以原生加粗格式标明的业务范围默认收起内部。浏览器实际核对后端 `modules/knowledge/`、前端 `features/knowledge/` 默认可见但内部收起；目录点击、键盘 Enter 及左侧引导线收起均通过。行距紧凑，业务目录有明确强调。
- 从规范和代码文件进入副屏，先显示实际维护的总体说明，再显示源文件；真实打开知识响应协议、维护规范和职责图源。点击具体成果文件不会把生成 HTML 当代码执行。
- 三类列表均在主屏进入详情；正文引用打开副屏。实际保留规范、图源及原有技能详情，返回目录、切换分类并进入另一篇架构文章后，三项对照仍保留；打开时的原上下文继续显示。每项读取的版本观察随接续上下文保留。
- 正常页面取消独立“来源核对”栏目与逐行一致徽标；变化、缺失与不可读仍就近提示并可打开具体文件。观察信息继续用于维护与反馈，不作为新的语义证明。
- 独立技术图使用原 Archify HTML，恢复主题、引导视图、查找、缩放及导出菜单；文内预览继续采用其嵌入模式。放大阅读只改变容器，未编辑外部技能或生成 HTML。真实打开系统图图源，确认说明与 JSON 内容。

### 检查结果

| 范围 | 结果 |
| --- | --- |
| 后端完整低成本回归 | 通过，`/tmp/knowledge-details-fast.log`；类型、单元、组件、契约与静态边界 |
| 知识读取与模块相关回归 | 24 项通过，`/tmp/knowledge-details-integration.log`；可选文件说明、成果文件关联、旧索引兼容、未知文件标识拒绝、原型策略不变、图示原文和下载隔离、跨类别同标识未关联读取拒绝 |
| 前端测试 | 31 项通过，`/tmp/knowledge-details-web-tests.log`；目录、导航、业务边界树、围栏不误解析、不同来源同路径不合并及带观察的地图完善指令 |
| 正式构建 | 通过，`/tmp/knowledge-details-build.log`；既有 Ant Design 公共包体积建议性告警保留，无新增依赖 |
| 随包与规范 | 13 个包含项、311 文件通过；严格校验通过、冲突预检 ready；`/tmp/knowledge-details-package.log`、`/tmp/knowledge-details-preflight.json` |
| 页面操作 | 真实生产托管构建，完成三类详情、折叠操作、说明先于原文、多个副屏保留、原生图示与放大查看；没有以原型或只读源码代替交互验证 |

点击原生 SVG 导出后，Archify 回执为 `format=svg`、`bytes=60251`、`canonical=true`，无生成错误。内置浏览器的下载事件监听没有返回回执；没有确认文件实际落盘，不宣称下载保存已验证。此限制不影响已观察到的原生展示、主题、菜单与数据生成。未穷举其他格式、浏览器或操作系统。

本轮仅修改宿主展示及阅读关联，既有图源和生成展示未改；来源摘要在实际核对后刷新。临时试用工作空间的方法已恢复更新，正式自举与发布状态不变。文件本身不代表认知目标完成，仍需用户继续试用判断解释是否有用。


## 阅读细节与维护闭环核查（2026-09-19）

新增 3 项实施完成，累计 33 项。预览继续使用原隔离工作空间和生产静态构建；没有合入、归档、发布或正式自举激活。

| 检查 | 当前结果 |
|---|---|
| 前端单元及构建 | 31 项通过，生产类型检查与构建成功；`/tmp/knowledge-polish-web-tests.log`、`/tmp/knowledge-polish-build.log`。原公共包体积告警保留，未增加依赖。 |
| 后端低成本检查 | `test:fast` 通过，`/tmp/knowledge-polish-fast.log`。后端实现未改变，复用前轮 24 项集成与装配边界结果。最后补充任务分流一句影响复核说明，由后续资源检查覆盖。 |
| 随包方法与规范 | 13 项 311 文件校验通过；严格规范检查与同任务工作树（Worktree）冲突预检 ready。第一次预检误用服务目录，因目标未初始化拒绝；随后以已核实工作根与 `--project product` 完成，没有初始化错误目录。 |
| 知识引用 | 两篇文章与两张地图 85 条原生引用均有真实文件和登记关联。新增当前变更维护规范与共用技能阅读定位；保留 2 个主题、10 份成果。 |
| 实际页面 | 技能正文默认可读；其他文件折叠、搜索、打开、返回；切换技能不带入旧正文；建设方法六项归并及分屏无溢出；图示高度减少且原生控件保留。完善指令包含实际范围、观察、整组维护及授权边界。 |
| 机制审查 | 静态追踪普通开发与 OpenSpec 交接、v3 授权和维护方法；明确未授权只提醒不写，已有授权连续完成。没有独立智能体行为实验，不以技能文字宣称模型必然遵循。 |

原 Archify 图源和展示未改，沿用既有图示验证；本轮只验证承载页面，不重复导出测试。提示来自实际读取后的观察，不代表后台持续监控；生成维护指令不代表已经执行。

## 收尾范围

用户于 2026-09-19 要求本试点收尾。33 项实施及相关验证已完成；同步四项规范并归档本变更，当前知识改为引用正式规范，保留原型和本文件中的历史验证过程。代码未新增修改时复用上述适用检查；归档迁移单独检查正式规范与引用。Git 交付、本机自举及安全清理以实际执行结果和任务完成记录为准；本次不发布版本，不完成父任务。
