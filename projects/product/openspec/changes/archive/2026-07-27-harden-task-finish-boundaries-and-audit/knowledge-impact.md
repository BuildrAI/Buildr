# Current Knowledge Impact Assessment

## 需要更新

- `openspec/knowledge/services/buildr.md`：补充产品强制停止边界、计时可信来源、只读审计和旧接口退役状态。
- `openspec/knowledge/flows/openspec-change-lifecycle.md`：补充 blocked 恢复规则和 Agent 责任边界。
- Task Finish 与 OpenSpec 受管 Skill/Component 文案：只暴露中文结果，并指向 `converge` / `audit`，不再推荐旧命令。

## 不需要更新

- 产品定位和核心模型不变。
- planner、projected validator、canonical applier、observer、receipt 的模块边界不变。
- OpenSpec 上游版本与 archive `--skip-specs` 约束不变。
