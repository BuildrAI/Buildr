## 1. OpenSpec 同步时序

- [x] 1.1 在 apply sidebar 与 pre-sync contribution 中明确 canonical spec 不得早于成功的 pre-sync 写入。
- [x] 1.2 在 Task Finish 中固定 `pre-sync → agent-driven sync → post-sync → archive --skip-specs`，并保持外部 OpenSpec CLI/Skills ownership。

## 2. Runtime projection 验证复用边界

- [x] 2.1 为精确可归因的 Buildr runtime projection/receipt delta 定义 closeout-metadata-only 条件和 focused checks。
- [x] 2.2 扩展 closeout fixture 与契约测试，覆盖允许复用和 fail-closed 情形。

## 3. 当前认知

- [x] 3.1 维护 Change Brief、OpenSpec lifecycle flow 与 glossary，并记录 knowledge impact。

## 4. 验证

- [x] 4.1 运行相关契约测试、OpenSpec strict validation 与 affected verification。
