## 1. 专业事实与来源

- [x] 1.1 扩展工作摘要可选当前节点、命令行和生成契约；用保留、清除、退回实现和冲突测试验证。
- [x] 1.2 审查当前替换时原子保留完整历史，扩展独立读取与契约；验证两类隔离、旧库只读、冲突、失败回滚和终态读取。
- [x] 1.3 任务材料与引用文档优先读取匹配工作树并标记来源；验证未提交差异、缺失文件、身份漂移、清理后的保留读取和路径边界。

## 2. 用户页面

- [x] 2.1 用现有主题构建简明工作路径与按节点直接材料列表，保留两个审查；验证无材料、简单任务、多个变更及修复回退的真实呈现。
- [x] 2.2 使用系统副屏直接阅读文档、原型、审查、验证和已有结果；验证切换节点、关闭、分隔线与来源展示。
- [x] 2.3 任务维护、工作摘要与答复统一使用抽屉，保持保存取消和冲突输入；验证不改变无关状态且保留阅读现场。

## 3. 协作与回归

- [x] 3.1 同步产品源技能与相关契约对节点记录、审查历史和 brief 用途的说明；核对消费者兼容与资产检查，复核知识影响并记录具体未授权维护建议。
- [x] 3.2 完成受影响后端集成、两个服务低成本回归、正式前端构建与生产托管浏览器验证，覆盖副屏/抽屉、窄屏及工作树来源，修正本次发现的问题。


知识影响已核对：任务系统文章中的人、智能体（Agent）与应用（Application）职责仍适用。后续知识维护建议仅涉及 knowledge/docs/overview.md 中任务页面与审查历史的说明，以及 knowledge/code-map/task-system.md 中新增页面组件的定位；本轮未扩展为知识主题重建。

验证已完成：后端快速回归（229 项单元、5 项组件、205 项契约及类型/架构/规范检查）、前端 70 项逻辑测试、相关集成与系统检查、全量 41 项浏览器检查通过；最后的表单重读保护调整另经任务浏览器流程复验通过。验证报告由任务验证（Task Verification）能力记录。


## 4. 列表与详情分屏及直接阅读修订

- [x] 4.1 复用现有资源预览与分屏，从列表打开任务详情并保留筛选/滚动，验证深链和关闭行为。
- [x] 4.2 压缩顶部标题、编码、目标与工作路径；默认任务需求并直接预览正文，各节点直接呈现内容，验证无额外打开文件步骤与无嵌套分屏。
- [x] 4.3 更新与复验受影响的列表、详情、文档与维护场景，并在生产托管页面核对实际布局。

本次分屏修订验证：正式前端构建、72 项前端逻辑检查、14 项任务结构检查通过；最终生产托管浏览器定向检查 task 2/2、workbench 15/15、service 10/10、articles 8/8 通过，浏览器错误为 0，检查使用的隔离工作空间均已清理。完整浏览器首轮因旧任务导航与文件关闭选择器失败并超时，已修正对应测试并完成上述受影响场景复验，不将首轮表述为全部通过。后端实现与源技能未因本次分屏修订变化，复用首次实现对应检查。实际页面已核对宽屏并排、窄屏阅读、默认需求正文及工作树来源。

## 5. 节点归并与必要内容修订

- [x] 5.1 将两类审查归入设计/实现，开发验证归入开发实现，用户确认归入收尾，保留专业阶段原义并更新路径映射。
- [x] 5.2 按用户判断需要删减重复和技术字段，保留有意义的结论、问题、来源及动作；修复同名文档区分。
- [x] 5.3 保留各节点材料/审查选择、滚动和关联阅读返回上下文，完成相关逻辑及实际浏览器验证。

四节点修订验证：正式前端构建、73 项前端逻辑测试、任务相关结构与架构检查通过；任务完整浏览器旅程及含长文档滚动位置的补充复验通过，工作台最终 15/15 通过，浏览器错误为 0。首轮工作台因“加入接下来”迁入更多菜单后的旧测试入口失败，已修正并复验。未改变共享分屏壳、后端工作树解析与专业数据模型，相关已有结果继续适用。

## 6. 总览、操作与列表及偏好性能

- [x] 6.1 实测偏好接口，批量读取标签、消除前端写后冗余请求并隔离同步材料读取，验证实时名称、并发顺序和范围隔离。
- [x] 6.2 顶部呈现必要总览，删除重复操作与人工进展维护，保留就地答复、收尾登记和明确接续指令。
- [x] 6.3 列表改为有表头的紧凑列式展示，验证分屏、窄屏、筛选、分页和回退保持。

总览与性能修订验证：后端快速回归（229 单元、5 组件、205 契约及类型和95项规范检查）、75项前端逻辑、9项偏好/工作台集成、5项有界读取与HTTP分发检查、Task文件接口系统3/3通过。生产托管浏览器 task 2/2、workbench 15/15、articles 8/8通过；任务场景覆盖58像素普通行、原型按需读取、工作树文件与文档刷新、阅读位置和收尾登记。测试中发现并修复了Markdown渲染晚于滚动恢复的问题，相关旧按钮及同步读取假设已更新。最后的总览跳转最新审查经实际浏览器核对。

本机同一并发材料读取场景（三次，预览示例）：旧偏好接口等待1138–1202毫秒，新服务13–21毫秒。两份隔离预览均使用真实HTTP入口；不把该测量作为通用性能保证。新预览服务地址记录在任务工作摘要，主系统未安装更新。

## 7. 以正文为中心重新设计

- [x] 7.1 将详情改为正文与进展属性分工，保留现有分屏，消除重复导航和专业状态。
- [x] 7.2 节点文件直接阅读、审查验证就地结论和报告历史可达，删除例行执行按钮。
- [x] 7.3 完成构建与相关检查，并在实际预览核对宽窄布局、节点切换及报告历史。

正文层级重排已完成：前端构建、75项逻辑检查、任务浏览器2项完整旅程通过，浏览器错误为0。宽窄副屏、默认需求、文档菜单、审查历史、验证返回与工作树正文经实际页面核对。首次浏览器检查发现移除固定导航导致长文档切换丢失位置，恢复固定节点导航后复验通过。后端未变，本轮不重复执行其已有检查。

## 8. 阅读与概况细节

- [x] 8.1 列表左右边距对齐目录表格，概况集中来源与全局清单。
- [x] 8.2 引用与专业结果改用共享阅读抽屉，保持正文、历史与链接上下文。
- [x] 8.3 取消焦点刷新，验证显式刷新、抽屉往返及既有任务流程。

细节调整验证：构建、75项前端逻辑、2项完整任务浏览器检查通过，浏览器错误0；新增窗口focus/visibilitychange不重读任务详情的请求检查，覆盖共享阅读抽屉、全局清单、文档继续阅读和手动刷新、审查历史与维护往返。

## 9. 统一节点阅读与任务级信息

- [x] 9.1 共用 Markdown 原文和阅读组件，保持文件链接与工作树来源。
- [x] 9.2 各节点采用左侧目录右侧正文，审查历次记录直接可见，收尾内容分项阅读。
- [x] 9.3 属性移至简介下方，侧栏直接呈现实施清单，登记完成移至任务级操作。
- [x] 9.4 构建并核对统一阅读、历史选择、手动完成和宽窄布局。

统一目录修订验证：构建、75项前端逻辑、任务浏览器2/2及包含技能页面的壳层浏览器7/7通过，浏览器错误0。实际页面核对 Markdown 原文一致性、常驻审查历史、宽屏实施清单与窄屏回流、完成登记入口。共享阅读器复用现有安全渲染，未改变后端或文件读取授权。

目录细节调整完成：编码移到标题下，删除顶部进展/下一步与独立审查对象区块；规范名两行，审查分组与子项缩进区分层级，保留两类多次记录。构建及任务浏览器2/2通过；修正用例重复点击关闭动画中的抽屉所导致的不稳定等待。最终缩进和布局经实际页面核对。

## 10. 逐页审查修正

- [x] 10.1 统一规范与审查二级结构、正文样式和必要状态呈现。
- [x] 10.2 去除引用重复标识，统一收尾与维护操作，并验证相关交互。

逐页修正验证：构建、75项前端逻辑、任务浏览器2/2通过，错误0；实际页面核对统一二级目录、验证状态去重、范围说明和引用文档。原文读取及专业数据不变。
