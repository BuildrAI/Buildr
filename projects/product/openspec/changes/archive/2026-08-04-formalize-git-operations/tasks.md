## 1. 唯一能力入口

- [x] 1.1 新增 `buildr.git-operations/v1` contract 与唯一 `git-operations` Skill，覆盖输入、授权、精确暂存、commit/push、push range、共享冻结和最小 Result
- [x] 1.2 迁移 workspace/package manifests、default binding、Task Finish optional dependency 与 Buildr 产品入口动态 routing
- [x] 1.3 保留 `git-worktree-provider/v1`，删除 `git-ops`、三项旧 Git contracts/bindings 和全部 active 双轨 residual

## 2. Current knowledge 与文档

- [x] 2.1 完成四份 delta specs，并准备 lifecycle roadmap / product current-state facts 的 canonical convergence 内容
- [x] 2.2 更新 capability contract 文档、bootstrap guide、CLI reference、render plan 与 Task Finish/task-worktree guidance
- [x] 2.3 完成 Change Brief、knowledge impact 记录和术语一致性检查

## 3. 验证资产

- [x] 3.1 更新 package static validation，明确覆盖独立 commit、独立 push、commit+push、无关 dirty、scope 外 range、push rejection、共享冻结和部分失败 evidence
- [x] 3.2 迁移 capability replacement、ownership recovery、bootstrap、Task Verification 与 capability adaptation tests 到唯一新 graph
- [x] 3.3 增加旧 ID/旧 Skill active residual gate，并验证新 Skill frontmatter 与两级 manifest description 完全一致

## 4. 交付验证

- [x] 4.1 运行 OpenSpec strict/proposal/implementation guards 和 focused Git Operations/package tests
- [x] 4.2 运行 Product affected verification，并修复全部回归
- [x] 4.3 完成 current knowledge reconcile / inspect、OpenSpec strict validation 与 deterministic convergence readiness
- [x] 4.4 完成 active residual audit，确认 Change 可由 Task Development 收敛到 canonical current specs 并 archive
