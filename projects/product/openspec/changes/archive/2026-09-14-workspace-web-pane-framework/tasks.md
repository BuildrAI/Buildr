# 任务清单

## 1. 组件底座

- [x] 1.1 新增统一抽屉壳组件（标识行 + 标题 + 副标题 + 关闭 + 可滚动正文 + 底部状态与主按钮区），基于 antd Drawer 封装，宽度统一 560px
- [x] 1.2 新增页面级页签条组件：页签（类型色点 + 标题省略 + 关闭）映射既有路由，状态按 workspace 维度持久于 localStorage，支持追加/激活/关闭/兜底回目录
- [x] 1.3 新增对象级页签条组件：页内状态驱动，全关时宿主右组退场
- [x] 1.4 新增双栏组容器与贯连分隔线组件：两组各自独立滚动，分隔线 9px 热区拖拽调宽（clamp(480px,44vw,960px)，区间 420px～62%），页签条高度处视觉连通，≤1440px 右组降级为浮动层

## 2. 壳层改造

- [x] 2.1 工作空间壳侧栏收敛为项目/服务/技能/设置四项平级同款行式入口（256px），移除项目树与所属服务展开
- [x] 2.2 壳层、侧栏与内容区统一白底，内容区接入左组页签条，顶部结构保持不变

## 3. 页面迁移

- [x] 3.1 项目目录页：纳入页面级页签，保留搜索/筛选/创建抽屉行为
- [x] 3.2 项目全景页：hero（身份 + 关键数字带 + 编辑入口）+ 服务卡片区 + 文档行区；点服务/文档在右组开对象页签，已打开对象在阅读位高亮；接入服务入口降为区域头部小按钮；移除跳走式链接
- [x] 3.3 服务目录页：纳入页面级页签，保留筛选与接入抽屉行为
- [x] 3.4 服务全景页：hero + 文档区 + 变更区；点文档/变更在右组开对象页签；编辑服务走抽屉（说明：服务暂无变更列表数据源，变更区不虚构，待有 API 后补）
- [x] 3.5 对象页签内容：服务（头部 + 标签 + README/AGENTS 子 Tab）、文档正文、每日演进，均复用既有只读渲染能力

## 4. 抽屉统一

- [x] 4.1 AgentActionDrawer、MetadataEditDrawer（项目/服务编辑）、SkillActionDrawer、SkillDetailDrawer 迁移到统一抽屉壳 DrawerShell，交互与视觉一致

## 5. 收尾与验证

- [x] 5.1 移除树形导航与旧并排布局的死代码与失效样式（shell-project-tree/toggle/link、shell-service-tree、shell-nav-heading/secondary/hint、旧 metadata-edit-drawer 壳样式）
- [x] 5.2 执行当前认知维护 assess：knowledge/docs/services/buildr-web.md 的壳层、导航视觉、抽屉三段已同步为新模型；其他文档无关联影响
- [x] 5.3 运行 buildr-web 的 typecheck、构建与既有测试全部通过（tsc -b 0 错误；npm test 10/10；vite build 成功）
- [x] 5.4 浏览器验证关键场景：页面级页签跨页生命周期✓、右组对象对照✓、分隔线拖拽✓、抽屉动作✓、≤1440px 浮动降级✓、控制台无错误✓（真实 workspace 手工验证）；browser-smoke 场景测试 11/11 通过
