## 1. 当前展示尺寸与响应

- [x] 1.1 从已安全读取的当前原生 HTML 主 SVG 提取可空 `diagramSize`，覆盖有效比例、主图识别、缺失及非法尺寸、非图示和图源不一致场景，相关读取测试通过。
- [x] 1.2 更新知识响应结构及生成类型，确认新字段受校验、图示 JSON `content` 仍为 `null`，隔离展示和相关 HTTP 回归通过。

## 2. 图示阅读尺寸

- [x] 2.1 传递尺寸并按容器实际宽度计算文内预览高度，计入当前嵌入模式的横向与纵向 `16px` 留白；空值和旧响应保留回退，前端类型检查及相关组件回归通过。
- [x] 2.2 在实际知识文章中验证任务总图和知识维护图的完整呈现及宽度变化，检查独立详情与放大阅读无宿主额外空白、裁切或交互退化，记录真实页面观察。

## 3. 当前知识与直接验证

- [x] 3.1 更新知识建设与维护的架构说明、代码地图（Code Map）及必要阅读关联，核对尺寸读取与知识内容只读边界；当前职责图和术语语义未变则保留并说明依据，最终更新实际来源观察。
- [x] 3.2 复核本次变更的实现、响应契约和知识表达一致，执行适用严格规范校验及收敛预检，保留明确未覆盖范围，不将静态规划检查当作运行验证。
