## 1. Onboarding 契约与 owner

- [x] 1.1 完成 `product-verification-quality` delta requirement，令 deterministic converge 将 development checkout onboarding 收敛为显式 Project bridge、development identity、sync/Launcher/Doctor 与 PATH 零 mutation。
- [x] 1.2 更新 verification ownership 文档、registry输入与 primary evidence说明，保留 repository onboarding 为 changed/focus/Release 专项而非完整 Candidate owner。

## 2. 跨平台 clean-checkout verifier

- [x] 2.1 用精确 Host Node与相邻 npm CLI显式准备 clean checkout依赖，并在 macOS/Windows执行同一个 `projects/product/buildr` bridge。
- [x] 2.2 用复用 Git object的 candidate snapshot覆盖当前 commit、tracked delta、未忽略 untracked files、临时 remote及sync后提交，移除全仓库复制和重新索引。
- [x] 2.3 加入 `buildr`/`buildr.cmd`双 sentinel并验证完整 lifecycle不改变 PATH默认 CLI。
- [x] 2.4 删除 legacy development PATH CLI installer/uninstaller、POSIX-only lifecycle test及System suite、Windows slice和static validation残留。

## 3. 验证与性能证据

- [x] 3.1 运行 OpenSpec strict、registry/owner契约、clean-checkout focus与changed plan，修复全部正确性或coverage缺口。
- [x] 3.2 收集多轮成功本地clean-checkout timing，记录Git snapshot与删除重复installer lifecycle后的wall-clock；最终source SHA的hosted Windows feedback由archive后的Formal Task Verification唯一持有。
- [x] 3.3 根据本Change成功timing中位数与波动范围保留或校准非阻断`budgetMs`和`schedulingCostMs`；不得用预算调整替代测试优化或必要场景。

## 4. 当前认知与收敛准备

- [x] 4.1 更新 Change Brief、knowledge impact与受影响的Service/verification当前认知，完成术语核对；没有真实长期影响的target标记not-applicable。
- [x] 4.2 核对最终spec、实现、registry、文档与timing evidence一致，勾选真实完成项并准备 deterministic converge/archive。
