## 1. 页面调整

- [x] 1.1 完整目标与列表摘要标签、项目服务导航和来源入口；检查长目标和链接。
- [x] 1.2 加宽目录和清单，固定标题、独立正文滚动；浏览器核对宽窄屏及长清单。
- [x] 1.3 确认时间置于标题下；核对已答复和待答复显示。

## 2. 验证与知识核对

- [x] 2.1 运行正式构建、已有前端检查及相关浏览器场景，确认四节点和阅读导航无回退。
- [x] 2.2 复核任务系统文章和代码地图；本次未改变职责、数据和文档来源选择，无需修改架构图与技能指引，在此记录核对结论。

验证结果：正式构建通过；75 项前端逻辑测试通过；现有任务浏览器流程通过；使用原任务目标、design.md 和 tasks.md 的隔离检查通过，覆盖 2560、1600、1024、390px，目录 180px、清单 320px、标题位置不随正文滚动变化、服务导航及待答复/已答复时间。任务系统文章与地图描述的职责及数据边界不变，架构文章与图无需修改；后续按用户决定更新任务目标的术语定义。

## 3. 用语统一

- [x] 3.1 统一目标用语和术语定义，修改搜索提示及表头，移除目标摘要前缀；以正式构建及浏览器文案核对验证。

本次补充：正式构建及隔离浏览器核对通过，表头、搜索提示和无目标前缀符合用户决定；前端展示代码已无“意图”残留。

## 4. 按需清单与内部阅读

- [x] 4.1 实施清单默认隐藏，右上角触发非模态浮窗；验证边界、开关、Esc、焦点和滚动保留。
- [x] 4.2 核心正文和目录内部滚动，迁移节点阅读位置恢复；验证长文档、节点切换、宽窄屏与展开阅读。
- [x] 4.3 完成构建与相关浏览器检查，更新当前预览及验证报告。

浮窗与内部滚动验证：正式构建、75项逻辑检查、完整任务浏览器2/2通过，错误0；新增默认隐藏、无焦点转移、Esc关闭、外层不滚动和节点阅读位置恢复检查。实际预览核对普通分屏/展开阅读/390px窄屏：浮窗始终贴齐详情右界，内部滚动和再次打开的位置保留，主页面仍可操作。修复验证发现的容器高度传递、按钮类型切换导致焦点丢失，以及旧表头断言。

## 5. 悬浮与固定模式

- [x] 5.1 调整入口和标签下方边界，实现350ms悬停关闭、固定占位及独立滚动，验证键盘/触屏和窄屏。
- [x] 5.2 完成相关浏览器与构建检查，更新原预览及验证记录。

悬浮/固定验证：正式构建、75项逻辑测试、完整任务浏览器2/2通过；新增520px浮窗、标签下方边界、悬停移入不误关、35%固定占位、离开保持和取消固定后自动隐藏检查。最终关闭时清理悬停状态与窄屏换行调整另外经构建及当前预览核对；390px无水平溢出，模式切换保留阅读位置。

## 6. 浮窗细节修正

- [x] 6.1 浮窗上沿贴齐标签分隔线，取消固定后按实际鼠标位置判断隐藏；通过贴线和静止鼠标回归检查。

细节修正验证：正式构建与完整任务浏览器2/2通过，错误0；回归覆盖上沿贴线、取消固定后鼠标静止700ms仍显示、随后移出自动隐藏。真实预览再次确认间隙0、静止保持、移出隐藏。隐藏判断改为实时命中检查，并补充模式切换缺少进入/离开事件时的指针移动检查。
