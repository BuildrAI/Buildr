# 栏目恢复与项目上下文交互

让菜单切换后继续刚才的查看位置，项目关联就地完成，并让服务详情与设置布局尊重当前上下文。

本次只处理四项用户反馈：栏目恢复、共用关联选择、项目视角省略反向项目区、设置对齐。项目列表显式进入仍是主页；全局服务仍展示关联项目。关联沿用当前版本校验，不新增 Git 或迁移操作。

来源与实现入口：应用壳导航及页面缓存、共用选择组件、项目关联卡片、资源详情和设置页面。现有代码地图需要更新导航解释，模型、术语和技术图无需调整。验收使用既有前端构建和浏览器检查，包含返回恢复、错误反馈和窄屏布局。

实现与验证已完成：前端构建及14项低成本检查通过；壳层浏览器7项、项目浏览器4项通过，包括栏目恢复与关闭标签、项目视角、直接关联、失败重试、取消保留草稿和设置宽度/窄屏。完整浏览器运行的其余通过项仍适用，旧“主页没有输入控件”断言已改为允许关联选择器并由项目检查通过。本变更已完成验证并同步归档，交付范围不含 Git 来源变更。

补充调整：元数据编辑抽屉不再二次确认放弃修改，取消、关闭、遮罩和 Esc 直接退出；未保存内容不写入。项目4项和服务5项浏览器检查、前端构建通过，实际预览确认关闭按钮与遮罩行为。
