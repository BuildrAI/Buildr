## 1. Task Finish 零差异恢复

- [x] 1.1 为 `task finish run` 增加只适用于 matching `adaptation-required` resume 的显式零差异确认参数与 canonical help/诊断
- [x] 1.2 实现 clean run-owned carrier 的零差异 adoption、verify 与 stable `already-contained` delivery，并保持 baseline、identity、source 与 target-race fail-closed
- [x] 1.3 分离 carrier actual delta paths 与冻结 Task Contribution activation paths，并让 retained activation 和 self-bootstrap 兼容消费

## 2. 产品资产与回归证据

- [x] 2.1 更新 Task Finish canonical Skill、package target 与相关 CLI/Result 结构验证
- [x] 2.2 增加零差异成功恢复、缺少确认、非法上下文、baseline/target 漂移及旧 Result 回退的 Unit/Integration/System affected 测试
- [x] 2.3 运行 focused affected 测试与 OpenSpec strict validation，确认新增测试能捕获旧的 `delivery-adaptation-missing` 行为

## 3. 当前认知与收敛准备

- [x] 3.1 完成 Change Brief、knowledge impact 与术语影响检查
- [x] 3.2 将稳定的恢复、activation 与自举事实同步到适用 current knowledge，并通过 reconcile
- [x] 3.3 完成全部 Change-owned checklist 与直接验证反馈，为 deterministic converge 准备
