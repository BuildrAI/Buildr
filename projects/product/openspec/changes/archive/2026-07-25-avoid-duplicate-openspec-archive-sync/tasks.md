## 1. Archive 选择规则

- [x] 1.1 在 `task-finish` 定义已手动同步且 post-sync 通过时的 `--skip-specs` archive 条件与记录要求。
- [x] 1.2 在 canonical spec 中加入对应 Requirement 与 fail-closed 场景。

## 2. 验证

- [x] 2.1 增加 Task Finish contract assertions，并运行受影响验证、OpenSpec strict 与 doctor。
