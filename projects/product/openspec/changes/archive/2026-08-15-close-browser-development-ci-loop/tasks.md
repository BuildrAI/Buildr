## 1. Browser 选择与冻结产物

- [x] 1.1 为 Browser dispatcher 实现 closed `selected` / `not-applicable` plan，并让 Browser-owned 0 selector 在 build/Chrome 前失败；补齐 package/config、非适用路径与旧假成功的 Contract 反例。
- [x] 1.2 增加临时 staging Web build 与 tracked `web-dist` exact comparison，调整 Vite outDir 注入和 Browser scripts，证明成功、drift失败、冻结目标不变与临时目录清理。

## 2. 平台感知的开发反馈

- [x] 2.1 在唯一 verification registry 中声明 Windows development owner，并实现 changed-path platform projection；补齐空投影、命中、依赖/资源复用及不附加 Fast admission 的 planner/CLI tests。
- [x] 2.2 重编排 `verify.yml` 的 dev feedback：macOS运行主要 affected/admission和条件 Browser，Windows并行运行平台敏感投影；更新workflow contract tests并确认 Candidate jobs、needs、runner与稳定 `Candidate gate` 不变。

## 3. 声明与当前认知

- [x] 3.1 在取得 Declaration Intake 精确授权后，更新 `verification.yml` 的 Browser applicability/proves，并用Contract test证明声明路径与dispatcher闭合对齐；保持 capability ID、requiredForDelivery、environment、effects与resource不变。
- [x] 3.2 收敛 `brief.md`、Buildr/Buildr Web Service knowledge与技术架构中的 Browser staging和development platform allocation事实；确认没有新增或冲突的长期术语。

## 4. 直接反馈与收敛就绪

- [x] 4.1 运行 Browser dispatcher/staging、planner/changed、verification entrypoint与workflow focused tests，保存反例证明并修复发现的问题。
- [x] 4.2 运行 Product Fast、当前 affected plan、OpenSpec strict与convergence preflight，确认 Change artifacts、实现、测试、声明和knowledge达到收敛就绪。
