# 测试验证框架修复

本次修复通用框架的声明路径根与当前指导，并对齐 Buildr 自身测试入口和源码选择归属。用户已明确授权实施；复用当前隔离工作树（Worktree），保留已完成知识分页成果。

知识影响（Knowledge Impact）：主框架文章说明服务路径、声明和执行职责，以六节点流程图（Workflow）表达使用主线与按需补建，并标注 Buildr 提供的技能（Skill）与用途。Buildr 采用文章按最终六个测试族说明产品实践，新增 `buildr-web-unit`、缩小 `buildr-web` 的测试根到浏览器实际范围；补齐“测试对象—真实入口—证明范围”表与真实用例，明确后端回归、候选入口不自动包含独立前端逻辑和浏览器检查。通用和产品代码地图（Code Map）分别说明通用维护能力与产品测试工具，核对根解析、选择责任和真实入口；保持框架方法与产品实例分层。

当前规范 `project-testing-guidance` 按已存在的源技能校准：缺失测试和漏选在授权范围内补齐，再核对当前内容与证据适用性后记录报告；保留定向检查不能冒充完整覆盖的边界，不恢复统一测试执行器或新增阶段门禁。

不新增问题清单知识文章。真实修复状态与检查结果由任务摘要和正式验证报告保存；规范增量同步但不归档，Git交付和发布不属于本轮动作。
