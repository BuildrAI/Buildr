## 1. 路径根与当前位置

- [x] 1.1 为 v4 地图增加可选 location，覆盖项目默认、服务成员、畸形字段与安全路径回归，保留旧声明不补写字段。
- [x] 1.2 在 inspect/validate/update 返回逐族当前位置，使用真实项目和外部服务登记验证不同根、局部不可用、工作目录逃逸及版本冲突零写入。

## 2. 指导与产品采用

- [x] 2.1 对齐当前规范、技能、字段参考、模板和契约，检查退役 v3／统一执行入口不再被当前指导要求，并通过规范和资源检查。
- [x] 2.2 将 Buildr 集成与系统测试声明对齐各自完整入口，经官方维护命令校验和回读，并验证既有入口契约。
- [x] 2.3 修复真实源码路径的必要测试选择，取得旧映射失败与修复后通过的回归证据，并实际运行相关测试组。

## 3. 认知与综合验证

- [x] 3.1 按最终能力更新主框架、Buildr采用说明和两份实现地图，核对图示仍适用，维护已核对来源并在开发预览检查。
- [x] 3.2 完成受影响共享调用、隔离CLI和适用低成本完整检查，按真实结果同步当前规范，验证增量与当前规范一致且无相关冲突。

## 4. 审查遗漏补齐

- [x] 4.1 按真实入口将前端逻辑测试拆为 `buildr-web-unit`，从 `buildr-web` 浏览器族移除前端逻辑测试根，经维护命令校验和回读六族声明，并验证入口与范围契约。
- [x] 4.2 完善产品采用文章及对应代码地图的“测试对象—真实入口—证明范围”表与真实用例，明确后端回归、候选入口、前端逻辑及浏览器检查的覆盖边界。
- [x] 4.3 按现行源技能维护 `project-testing-guidance` 的定向反馈、稳定入口交接和补测职责，清除旧正式执行模型残留，并通过当前规范与既有变更的严格校验。
- [x] 4.4 核对受影响知识来源摘要与开发预览，检查通用框架、六节点流程图、六族产品实例和真实入口之间的一致性，完成受影响检查。
